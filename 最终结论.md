# 最终结论（对 结论1.md / 结论2.md 的核验与修复收口）

> 日期：2026-09-18
> 方法：逐条对照源码复核；关键论断独立复现——重跑结论1 的两组变异实验、为结论2 的 4 个 P0
> 各写可执行回归测试（先证「旧实现下红、新实现下绿」）；P1/P2 抽样核对定位与机制。
> 基线：修复前 42 文件 / 420 用例全绿 → 修复后 **45 文件 / 440 用例全绿**，`tsc --noEmit` 通过，
> `eslint src tests` 干净。

## 一、核验结果：两份结论均成立，无需勘误

### 结论1（本轮改动审查 + 测试质量）

| 论断 | 核验 |
|---|---|
| keepAlive `.catch()` 修复合理 | ✅ 属实。`poke()` 在 `setInterval` 裸调用，reject 即 unhandled rejection |
| **删掉 `.catch()` 后 keepAlive.test 7/7 全绿（空转用例）** | ✅ 独立复现：施加同一变异 → 7/7 绿。指控成立 |
| 空转根因是 vitest 下 `process.on("unhandledRejection")` 收不到事件 | ✅ 采信（该断言恒真与变异全绿互证） |
| ttsPlayback `finally` 无覆盖（头部注释虚称已覆盖） | ✅ 属实：全文件对 keepAlive 零断言，仅注释宣称 |
| 4 处 `beginKeepAlive` 均配对 `endKeepAlive` | ✅ grep 复核一致 |
| TokenBucket 除零不可达（interval 钳 ≥50ms） | ✅ `translate.ts:253-256` 复核一致 |
| prettier 基线债务（约 53 文件） | ✅ 方向属实；本轮顺带把全部被改动文件格式化，违规数降至 48 |

### 结论2（全仓库审查）

**4 个 P0 全部成立**，且本轮修复时以回归测试反向确认了旧实现下的可触发性：

- **P0-1（孤儿译文块）** ✅ 成立。机制补充：`wrapContainer` 会把原容器**搬进** `.it-wrap`，
  站点回收节点是 `container.remove()`——包裹层连同译文留守文档，而 `restore()` 对失连容器
  `continue` 后 `byContainer.clear()`，残块永久脱离索引。触发点不止 restore：
  `clearTranslatedMode`/`setMode` 的失连驱逐在 restore 之前就会先丢索引（结论2 已预见）。
- **P0-2（重入排队覆盖还原）** ✅ 成立。`finally` 重放无任何门槛 + `translateAll()` 开头
  无条件清 `userRestored`。补充细节：`restore()` 本身会清排队标记，因此违规窗口是
  「还原之后、在跑任务收尾之前到达的自动触发被吸收为排队」——修复按此建模。
- **P0-3（sessionId 跨页互杀）** ✅ 成立。`Map<number, Set<AbortController>>` 裸数字为键，
  `all_frames: true` 下各 frame 代次都从 0 起；`page-summary` 共用键空间。协议层无需改动：
  `sender.tab/frameId` 由浏览器盖章，天然可作隔离维度。
- **P0-4（导入清空设置 + 明文 Key 被误解密）** ✅ 成立。`sanitizeImportSettings` 以
  DEFAULT 为底全量落盘；导入路径不过 `encryptApiKey` 而读取无条件 `decryptApiKey`。
  `settingsCompat.test.ts` 恰用含 `-` 的 Key（atob 抛错回退才幸免）——覆盖面盲区属实。

**P1/P2 抽样全部与代码相符**：P1-15（哨兵分支无条件 `stripIndex`）、P1-17（ja/ko 无密度阈值）、
P1-18（模式整体小写而 `url.pathname` 原样比对）、P1-19（多 `*` 拼正则的灾难性回溯）、P1-21
（`mergeSettings` 外泄 DEFAULT 数组引用 + popup 就地 push）、P1-10（白名单不支持通配符且无提示）、
P2（manifest `scripting`/`activeTab` 全仓库零使用——grep 复核为空）。
**两条误报澄清成立**：`input.ts:80-100` 敏感页校验确实存在；不追加改动。

结论2 的其余 P1/P2 条目未逐条复测（静态确认类居多），修复时以「不与其冲突」为准；清单见第四节。

## 二、本轮修复内容

| # | 问题 | 修复 | 回归测试 |
|---|---|---|---|
| 1 | P0-1 孤儿块 | `renderer.ts` 新增 `discardContainer()`（原位快照还原 → 解包 → 清标记，脱离文档同样有效）；`restore()`、`setMode`/`applyTranslatedMode`/`applyTextOnlyMode`/`clearTranslatedMode` 的失连驱逐路径先清理再丢索引；站点已摘走的原文**不复活** | `orphanRestore.test.ts`（3 例：留守包裹层清除、setMode 驱逐、回收节点重挂不带标记） |
| 2 | P0-2 重入覆盖还原 | `translateAll(userIntent?)`：仅显式用户入口（工具条、Alt+T、换语言）传 `true` 并清 `userRestored`；排队记录意图标记；`finally` 重放须过 `userRestored` 门；bfcache 补译加还原意愿守卫 | `restoreReentry.test.ts`（3 例：自动排队被拦、用户意图放行、无还原时照常重放）；`autoTranslatePerSite` 用例按新契约更新 |
| 3 | P0-3 跨页互杀 | 新增 `background/sessionRegistry.ts`，以 `tabId:frameId:sessionId` 为键；`index.ts` 的 translate/page-summary/cancel 全部改走登记表（content 协议零改动） | `sessionRegistry.test.ts`（5 例：跨 tab、跨 frame、摘除不误伤、无 sessionId 不参与、无 tab 发送方隔离） |
| 4 | P0-4 导入清空 + 密钥链路 | `importSettings` 改为**以当前设置为底**合并文件补丁；不含任何已知设置段的文件直接拒绝（package.json 等误选即刻报错）；导入 Key 归一为明文后统一走 `saveSettings` 加密落盘（维持「盘上恒为密文」不变量）；`mergeSettings` 深拷贝 base（P1-21）且文件未提 `backupApi` 时保留现值 | `settingsCompat.test.ts` 新增 6 例；`ttsSynthesize` 导入用例按新语义更新（mock 补持久化） |
| 5 | 结论1：空转用例 | 重写为行为契约断言：stub promise 观察 `.catch` 是否挂上——删 `.catch()` 即红（旧写法恒绿的根因已在用例注释中记录） | `keepAlive.test.ts` |
| 6 | 结论1：finally 无覆盖 | `vi.mock` keepAlive 为 spy，断言成功/抛错/两次失败三条路径 begin/end 严格配对 | `ttsPlayback.test.ts`（+3 例） |

**对用户可见的行为变化**：部分导入不再清空未提及的设置（只更新出现的字段）；误选非设置文件
会明确报错；用户还原后 bfcache 回跳不再自动译回；Alt+T / 工具条 / 换语言等显式入口行为不变。

## 三、修复自证：变异测试 6/6 捕获

每处修复反向施加变异，确认对应回归测试变红，再全部还原复验（440 全绿 + tsc + eslint）：

| 变异 | 结果 |
|---|---|
| 删 keepAlive `.catch()` | 🔴 新用例红（对照组：旧测试 7/7 绿——正是要修的空转） |
| 删 ttsPlayback `finally` | 🔴 3 例红 |
| 去掉 engine 重放门控 | 🔴 restoreReentry 红 |
| 去掉 clearTranslatedMode 驱逐前清理 | 🔴 orphanRestore 2 例红 |
| sessionRegistry 键退化为裸 sessionId | 🔴 3 例红 |
| normalizeImportedKey 退化为恒等 | 🔴 密文往返红 |

## 四、未修清单（遗留债务，未做任何改动）

- **P1（结论2 第 1-20 条中除已修外者）**：建议按组收口——
  ① 渲染可逆性组（P1-1~5，快照/过滤标记时点错位、`data-it-inside` 只写不清、`textTranslations`
  无上限）；② 状态语义组（P1-7/8 `suppressPageState` 吞 `done` 致按钮反转——与本轮 P0-2 同文件，
  下轮动 engine 状态机时一并处理）；③ 请求放大组（P1-11 abort 穿不透 sleep、P1-12 批次硬失败后
  逐段重发、P1-16 TTS 无超时无取消）；④ 共享模块组（P1-15 stripIndex 误剥合法编号、P1-17 ja/ko
  阈值、P1-18/19 大小写与 ReDoS、P1-10 白名单通配符静默失效、P1-20 数值范围校验）。
- **P2 全量**：见结论2 附录；其中两项成本极低可顺手做——移除 manifest 未使用的
  `scripting`/`activeTab` 权限（已复核零使用）、发布管线排除 `.map`（sourcemap 随
  `web_accessible_resources` 外泄）。
- **P0-4 的历史残留**：修复前以明文落盘过、且恰为合法 Base64 形态的 Key 仍会显示为乱码，
  需用户在设置页重输一次（无法从存储侧无损判别历史明文，不做自动迁移）。

## 五、提交建议

结论1/结论2 属过程性文档，修复收口后可按仓库惯例（「删除已完结的过程性文档」）在下次
文档整理时移除或归档至 `docs/`；本轮先原样保留以便对照。
