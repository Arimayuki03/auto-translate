# 代码审查结论

审查范围：工作区未提交改动（`src/background/keepAlive.ts`、`.gitignore`）与 5 个新增测试文件
（`keepAlive`、`rateLimiter`、`placement`、`i18n`、`ttsPlayback`）。

验证结果：420 个测试全绿（42 个文件）、`tsc --noEmit` 通过、`eslint src tests` 干净。
审查过程中对源码做的变异实验均已还原，工作区状态与审查开始时一致。

## 一、结论

代码改动本身**正确且值得合并**。但用变异测试（故意破坏源码、看测试是否会红）验证测试质量时，
发现其中一条关键用例是**空转的**：本次修复正是它声称要守护的对象，而删掉修复后它依然全绿。

## 二、改动审查

### 1. `keepAlive.ts` — 给 `getPlatformInfo()` 加 `.catch()`

修复合理。`getPlatformInfo` 在扩展上下文失效（SW 即将被回收）时会 reject，而 poke 是 `setInterval`
里的裸调用，产生的是 unhandled rejection——每 20 秒一次刷屏，反而把保活逻辑自己想解决的
「worker 被回收」诊断信息淹掉。改动只有一行，注释也说明了动机。

顺带确认了这条链路的其他部分是干净的：全项目 4 处 `beginKeepAlive` 调用点
（`background/index.ts` 三处、`ttsPlayback.ts` 一处）**全部**在 `.finally()` 中配对 `endKeepAlive`，
无引用计数泄漏。

### 2. `.gitignore` 加 `.v2c/`

本地工具目录，合理。

## 三、测试质量：有真问题

变异检验结果：

| 测试文件 | 变异是否被捕获 |
|---|---|
| `placement.test.ts` | 去掉偏移补偿 → 2 红；忽略缩放系数 k → 1 红 |
| `rateLimiter.test.ts` | 容量下限不钳制 → 1 红；configure 补满令牌 → 2 红 |
| `i18n.test.ts` | en 表删键 → 4 红 |
| `ttsPlayback.test.ts` | 去掉重试逻辑 → 2 红；**去掉 finally → 全绿** |
| `keepAlive.test.ts` | **去掉本次修复的 `.catch()` → 7 个全绿** |

### 主要问题：`keepAlive.test.ts` 没有守住本次修复

把 `getPlatformInfo().catch(...)` 改回裸调用后，包括那条专门写的「getPlatformInfo reject 被吞掉：
不产生 unhandled rejection」在内，7 个用例**全部通过**。

原因不是测试逻辑写错，而是环境不配合：单独探针验证过，vitest/Node 环境下
`process.on("unhandledRejection")` 监听器在微任务和宏任务之后都收不到事件（计数始终为 0）。
也就是说这条断言恒真——它在测一个当前环境里根本不会触发的机制。

**这条用例目前是装饰性的。** 它给了「这个修复已被验证」的错觉，但实际上就算有人把 `.catch()`
删掉，CI 也不会报警。

### 次要问题：`ttsPlayback` 的 `finally` 无覆盖

去掉 `ttsPlayback.ts:93-95` 的 `finally { endKeepAlive() }` 后 15 个测试全绿。这恰好是引用计数
泄漏——和本次改动同一条链路，泄漏后 `active` 永远归不了零，保活定时器永不停止。当前实现是对的，
但没有测试能挡住这个回归。

（该文件头部文档声称「finally 中 endKeepAlive 保底执行」已覆盖，实际没有对应断言。）

### 附：令牌桶边界是安全的

`TokenBucket` 的 `rate` 可能为 0 或 `Infinity`（`rate = 1000 / interval`）。已核实调用方
`translate.ts:253` 把 `interval` 钳到最小 50ms，因此生产路径上 `rate` 恒为有限正值，不会出现除零。
探针确认极端值下行为：`rate=Infinity` 时等待返回 0；`rate=0` 时返回 `Infinity`（不可达路径）。

## 四、关于 prettier

`npx prettier --check src tests` 报 55 个文件不合规。stash 后跑基线确认是 **53 个**——这是仓库
既有的历史状态，本次改动只增加了 2 个新文件，不是新引入的问题。

因为已有 53 个文件都没格式化，不建议顺手跑 `--write`（会制造一个巨大的无关 diff）。若要收口，
应单独一次提交专门做格式化。

## 五、建议

按优先级：

1. **修 `keepAlive.test.ts` 那条 unhandled-rejection 用例**。不要依赖 Node 进程的
   `unhandledRejection` 事件（在 vitest 下不可靠）。改成直接断言行为契约：mock 一个返回
   rejected promise 的 `getPlatformInfo`，断言其返回值被消费（如 `vi.spyOn` 观察 `.catch` 被调用），
   这样才能真正防住删掉 `.catch()` 的回归。
2. **给 `ttsPlayback` 补一条 finally 用例**：让 `sendMessage` 抛错，断言保活计数归零
   （spy `endKeepAlive`，或断言失败后心跳停止）。
3. 可选：`rateLimiter` 的 `Math.ceil` vs `Math.floor` 未被覆盖——差异只有 1ms 等待，
   影响可忽略，不急。
