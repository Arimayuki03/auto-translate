# 未来方向：对照 read-frog（陪读蛙）的开发与优化路线

> 日期：2026-09-14（当日完成 P0 一揽子后更新）
> 参照对象：`read-frog-main/`（Read Frog 陪读蛙 v1.45.3，WXT + React + MV3，src 约 21.1 万行，其中测试 5.5 万行；Chrome/Edge/Firefox 上架，9 语言 UI）。
> 本项目：auto-translate（原生 TS 无框架，content 约 56KB，55 文件 580 用例）。
> **P0 一揽子（样式主题 / 属性翻译 / Microsoft 免费通道 / 缓存 TTL / 超时缩放与请求去重）已全部落地**（2026-09-14）。
> **P1 主体（划词流式 / iframe / Shadow DOM / 悬停翻译 / 自定义 prompt / 巨型段落拆分 / 免译哨兵）已并行落地**（2026-09-14）。

## 一、定位差异——先说清楚不追什么

read-frog 的定位是「AI 语言学习产品」：文章分析、词句讲解（CEFR 分级）、生词本（Notebase）、TTS 朗读、账号体系、数据分析。我们定位是「轻量纯翻译工具」。

**不学它的**：React/Jotai/TanStack 技术栈、monorepo/nx/changesets 工程链、账号与激活体系、行为分析、sidepanel。这些是团队产品化的配置，照搬只会丢掉我们「单文件、零依赖、秒加载」的差异化优势。我们的目标不是变成 read-frog，而是**把一个轻量翻译工具的核心体验做到不输它，同时保留它没有的轻量与可控**。

## 二、能力对照表

| 能力 | 我们 | read-frog | 差距结论 |
|------|------|-----------|----------|
| 双语 / 仅译文切换 | ✓ | ✓ | 持平 |
| 译文样式主题 | **4 种（灰字/描边/虚线/模糊）+ 自定义 CSS** | 8 种 + 自定义 CSS（8KB） | 已补齐常用项，够用 |
| 批量协议 | 30 单元/批 + 哨兵/逐行双协议 + 三级降级 | 4 段/1000 字符 + `%%` 分隔 + 次数不匹配重试 | 我们的批量更大更省 token，降级链路不输 |
| 视口懒翻译 + 预加载 | ✓（300px margin） | ✓（margin/threshold 可配） | 持平；可加配置 |
| 时间片遍历 + 让出主线程 | ✓（12ms） | ✓（同思路） | 持平 |
| 请求限流 | 令牌桶（间隔可配置）+ 429 队列冷却 + **跨标签页请求去重** | 令牌桶 8 req/s + 优先级队列 | 持平；优先级队列见 P2 |
| 持久缓存 | **TTL 7 天（可配）+ alarms 每日清理 + 条目数上限** | Dexie sha256 + 7 天过期 + alarms 清理 | 已补齐 |
| 上下文感知 | 标题/描述/正文摘要截断 + **LLM 页面摘要（缓存、按字数阈值、默认关）** | defuddle 正文 markdown + **LLM 摘要（缓存）** + token 注入 | 已补齐（无 defuddle，用 TreeWalker 轻量取词） |
| 服务商 | 5 格式（openai 兼容覆盖绝大多数中转）+ **googlefree / microsoft 双免费通道（429 自动互切）** | 33 个（含 google/microsoft 免 key、deeplx、deepl） | 免费双通道已齐，不必追数量 |
| 流式输出 | ✓ 划词气泡流式（Port + SSE，关闭即中止） | 划词/自定义动作流式（端口流） | 已补齐 |
| HTML 属性翻译 | **placeholder/title/alt/aria-label 全属性** | 全属性标记 | 已补齐 |
| 划词翻译 | 气泡 + 流式输出 + **朗读** | 流式 + 词典 + 朗读 + **自定义动作** + 触发方式可配 | 词典/自定义动作按需增强 |
| 输入框翻译 | ✓（含 input 事件派发） | ✓ + draft-js/slate 编辑器适配 | 持平（编辑器适配按需） |
| iframe 翻译 | ✓（all_frames 全 frame 注入，子 frame 独立引擎） | ✓（webNavigation + 全 frame 注入） | 已补齐（工具条暂不跨 frame） |
| Shadow DOM | ✓（open root 提取 + adoptedStyleSheets 样式） | ✓（查找/样式注入） | 已补齐（动态 shadow 靠重扫） |
| 字幕翻译 | ✗ | ✓（YouTube timedtext 拦截 + 分段管线 + SRT 导出） | 大工程，P2 差异化 |
| TTS 朗读 | ✓（划词气泡朗读：Edge TTS 免费 + offscreen 播放 + 熔断） | ✓（Edge TTS 免费 + offscreen 播放 + 熔断） | 已补齐基础形态（语音/语速可配，无分块预取） |
| 站点规则 | 黑白名单 + 每页禁用 | **内置站点规则库**（tag 级自定义/强制块行内） | P1 |
| 自定义 prompt | ✓（附加指令，协议段强制保留） | ✓（多套内置 + 用户自定义） | 已补齐基础形态 |
| 术语表 | ✓（⟦n⟧ 占位保护） | 无显式术语表 | **我们的优势** |
| 错误诊断 | 端到端脱敏诊断 + 工具条复制 | 原位错误组件 + toast | 持平，各有侧重 |
| 配置管理 | 导入/导出 JSON | 多快照备份 + Google Drive 同步 | 导入导出够用 |
| UI 语言 | 纯中文 | 9 语言 | 上架商店前补 en |
| 测试 | 580 用例（协议/调度/取消/属性/样式/流式/shadow/悬停/摘要/路由/缓存回归） | 260 文件 5.5 万行（含真实站点用例） | 按需补站点规则用例 |

## 三、小而美的借鉴项（低成本，先做）

这些是 read-frog 里「几十行就能搬过来」的点子：

1. ✅ **缓存 TTL + 定期清理**（已完成，见 P0 一揽子）。
2. ✅ **批量超时按字符数缩放**（已完成：`base + 15ms × 字符数`，封顶 120s）。
3. ✅ **跨标签页请求去重**（已完成：文本+语言在途去重，等待者不随属主会话中止而失败）。
4. ✅ **巨型段落拆分**（已完成：提取阶段按句子边界均衡拆 chunk，中文句读必切、英文 `3.14`/`e.g` 不误切）。
5. ✅ **`{{NO_TRANSLATION_NEEDED}}` 哨兵**（已完成：LLM 批量免译显式回哨兵，解析严格相等映射回原文；免费通道解析兜底）。
6. ✅ **视口优先调度**（2026-09-14 落地）：`translateUnits` 内批次按「距视口距离」动态出队
   （每次出队前按当前视口重选最近的批，滚动后未发批次跟随用户位置），先完成的批先渲染
   （消除队头阻塞）；并发度仍由 FETCH_WINDOW 钳制。jsdom/rect 全 0 环境退化为 DOM 顺序。

## 四、功能路线图

### P0：翻译核心体验补齐 —— ✅ 已全部完成（2026-09-14）

| 项 | 状态 | 说明 |
|----|------|------|
| 译文样式主题 | ✅ | 灰字 / 描边 / 虚线下划线 / 模糊 + 自定义 CSS（`translate.style` / `translate.customCss`） |
| HTML 属性翻译 | ✅ | placeholder / title / alt / aria-label 并入统一属性管线（`translate.translateAttributes`） |
| 免费通道二梯队 | ✅ | `microsoft` 免 key 通道 + googlefree ↔ microsoft 429 自动互切 |
| 缓存 TTL + 清理 | ✅ | `cache.ttlDays`（默认 7 天）+ chrome.alarms 每日清理 + 设置页缓存管理 |
| 批量超时缩放 + 请求去重 | ✅ | 超时 = base + 15ms/字符（封顶 120s）；跨标签页按「文本+语言」在途去重 |

### P1：体验增强 —— ✅ 已全部完成（2026-09-14：并行一揽子 + 站点规则库 + UI 英文化）

- ✅ **划词翻译流式输出**：Port（`it-stream`）+ provider SSE（openai/anthropic/gemini/ollama），rAF 节流逐字渲染，
  关闭气泡即中止（复用 signal 管线）；未产增量时按既有降级链回退。
- ✅ **iframe 支持**：manifest `all_frames`，子 frame 独立引擎/守卫/autoTranslate；Alt+T 经 tabs.sendMessage 广播。
  已知限制：工具条按钮暂不作用于子 frame（跨 frame 同步需 background 新消息，可作后续小项）。
- ✅ **Shadow DOM 提取**：open root 遍历 + adoptedStyleSheets 样式注入（按模式/主题直出）。已知限制：MutationObserver
  不观察 shadow 内部，动态 shadow 内容由下次全量/点击扫描覆盖。
- ✅ **站点规则库**（2026-09-14 落地）：`src/shared/siteRules.ts`——12 条内置规则（全局数学公式区 +
  GitHub/Discord/Reddit/YouTube/HN/StackOverflow/ChatGPT/Medium/Notion/Substack/Wikipedia 的排除区，
  选取原则「只排明确无翻译价值的区域」）+ 用户自定义 JSON 规则（每行一条或整段数组：
  excludeSelectors / excludeTags / forceBlockTags / excludeMatches / enabled）+ 设置页勾选禁用内置规则。
  域名通配（*.example.com、www.amazon.*）与路径前缀匹配；接入提取器（排除区子树/标签跳过、
  强制块级锚定）、Shadow DOM 宿主判定与属性翻译过滤；导入走 sanitizeSiteRules 逐字段校验（封顶 100 条）。
- ✅ **自定义翻译 prompt**：`api.customSystemPrompt` 附加指令注入（协议段强制保留），设置页 textarea。
- ✅ **悬停翻译**：块级元素「译」角标 + 单段翻译（`engine.translateElement`，页面状态钳制防整页补译）。
- ✅ **UI 英文化**（2026-09-14 落地）：`src/shared/i18n.ts` 中英双语表，content/popup 用户可见
  字符串全部收口 `t(key)`；按浏览器语言自动选择（zh* → 中文，其余英文），上架前就绪。

### P2：差异化大功能（按兴趣选做，每个都是独立里程碑）

- **YouTube 字幕翻译**：拦截 timedtext 请求拿原始字幕 → 分段 → 按播放时间轴预译 → 双语字幕覆盖 + SRT 导出。工程量大（平台适配 + main-world 拦截器 + 字幕渲染），但做完就是插件最亮的差异化点，read-frog 的实现可逐模块参考。
- ✅ **TTS 划词朗读**（2026-09-14 落地）：划词气泡「朗读」按钮 + Edge TTS 免费合成（HMAC 签名换端点令牌 →
  区域端点 POST SSML，`src/background/edgeTts.ts`）+ offscreen 文档播放（`public/offscreen.html/js` +
  `src/background/ttsPlayback.ts`）+ 失败熔断（5 次/10min → 开 15min）。声音按目标语言自动选（16 语言，
  可显式指定）、语速可调；关闭气泡即停。已知限制：单次合成上限 3000 字符（不分块续播）、无声音列表拉取。
- ✅ **LLM 上下文摘要**（2026-09-14 落地）：长文页（正文 ≥ `translate.summaryMinChars`，默认 6000，
  0 = 不限）整页翻译期间异步请 LLM 生成 2-3 句文章摘要注入上下文（`TranslateService.generatePageSummary`
  + `SummaryCache` 按页缓存，键掺通道格式/模型变体）。首批请求不等待（先带原文截断上下文），
  摘要返回后的批次自动携带；失败静默回退原文截断，免费通道自动跳过。默认关闭，设置页开启。
- ✅ **语言检测增强**（2026-09-14 落地）：`src/shared/langDetect.ts`——强制源语言 > `<html lang>` >
  字符启发式（拉丁文本不猜交端点自动检测）；随上下文注入 LLM 提示词、免费通道作 sl/from 参数；
  设置项 `translate.forceSourceLang`（默认自动）。

### P2：工程健壮性

- ✅ 优先级请求队列（视口距离排序）——已随「视口优先调度」落地（2026-09-14，content 侧批次出队排序）。
- ✅ 站点规则回归测试（2026-09-14 落地）：`tests/fixtures/` 收录 GitHub/YouTube/HN/StackOverflow
  简化真实 DOM 片段，走生产同构路径断言提取边界（9 用例）；另有 siteRules/siteRulesExtraction 26 用例。
- ✅ e2e 冒烟（2026-09-14 落地）：`tests/e2eSmoke.test.ts`——playwright persistent context +
  Chromium 新 headless 加载 dist/，route mock 免费通道，覆盖翻译/还原/切模式/SW 启动（3 用例），
  无浏览器环境自动跳过。

## 五、read-frog 源码索引（做对应功能时直接看）

| 功能 | 位置 |
|------|------|
| 请求队列/限流/重试 | `src/utils/request/request-queue.ts`、`retry-policy.ts`、`cancellation.ts` |
| 批量队列与解析 | `src/utils/request/batch-queue.ts`、`entrypoints/background/translation-queues.ts` |
| DOM 遍历/过滤 | `src/utils/host/dom/traversal.ts`、`filter.ts`、`constants/dom-rules.ts` |
| 段落切分/巨型段落 | `src/utils/host/translate/dom/paragraph-segmentation.ts` |
| 属性翻译标记 | `src/utils/host/translate/html-attribute-markers.ts` |
| 译文插入与防跳动 | `src/utils/host/translate/translation-insertion.ts`、`translation-node-preset.css` |
| 上下文/摘要 | `src/utils/host/webpage-context.ts`、`content/summary.ts` |
| 免费翻译 API | `src/utils/host/translate/api/`（google/microsoft/deepl/deeplx） |
| 字幕翻译 | `src/entrypoints/subtitles.content/`、`interceptor.content/`、`src/utils/subtitles/` |
| 站点规则 | `src/utils/site-rules/` |
| 流式网关 | `src/entrypoints/background/background-stream.ts` |

## 六、推荐执行顺序

1. ✅ **P0 一揽子**（样式主题 + 属性翻译 + Microsoft 免费备份 + 缓存 TTL + 超时缩放/请求去重）——已全部落地（2026-09-14）。
   待用户实机验证（DeepSeek 实测 / 免费通道 / 还原重翻 / 请求间隔）。
2. ✅ **P1 主体**（划词流式 + iframe + Shadow DOM + 悬停翻译 + 自定义 prompt + 巨型段落拆分 + 免译哨兵）——已并行落地（2026-09-14）。
   待用户实机验证（shadow 站点 / iframe 站点 / 悬停角标 / 气泡流式）。
3. ✅ 站点规则库 + 视口优先调度。——已落地（2026-09-14），见上方 P1/P2 条目。
4. ✅ UI 英文化——已落地（2026-09-14）。
5. 按兴趣选：字幕翻译 / TTS（✅ 2026-09-14 已落地）/ LLM 摘要（✅ 2026-09-14 已落地）。

## 七、待实机验证清单（用户执行，单测/e2e 已覆盖）

- **第三方 API**：用真实第三方站点（DeepSeek / OneAPI / 中转）复测正常翻译；确认 BaseURL 拼接
  （`buildApiUrl` 兼容尾斜杠/重叠路径）、请求头、Key 解密与历史版本一致；确认批量哨兵提示词不被拒
  （已用「默认逐行 + 仅配置启用哨兵」规避）。
- **设置兼容性**：历史 `version: 4` 设置升级后字段原样保留；导入/导出后 API Key、BaseURL、模型正确恢复。
- **UI 英文化 / 语言检测**：英文浏览器 UI 下工具条/气泡显示英文；强制源语言下拉对误判页面生效。
- **站点规则 / 视口调度**：GitHub 文件列表/页头不出译文；YouTube 字幕条不受影响；自定义规则刷新生效；
  整页直译先看到当前屏幕译文，滚动后新批次跟随阅读位置。
- **LLM 摘要**：长文页（≥6000 字）请求多一次、术语一致性提升；关闭 / 短页 / 免费通道无额外请求。
- **TTS 朗读**：真实页面划词朗读有声，播放中停止、关气泡即停、连续划词不打架；Edge TTS 限流提示友好。
- **P0 一揽子实机**：DeepSeek 翻译、googlefree 提速、还原后重翻不缺段、还原时在途请求 cancelled、请求间隔设置生效。
- **上下文**：确认上下文注入不影响第三方 API token 限制；敏感页（登录/支付）不注入上下文。

## 八、审查留观项（已知限制，暂不处理）

> 来自历轮代码审查（2026-09-14 ~ 09-18，报告原文见 git 历史：docs/代码审查报告.md 与仓库根 结论1/结论2/最终结论.md）。

- tokenrhythm CSRF 403：已确认非插件回归（服务端拦截扩展类请求），已搁置；如需支持再评估 `declarativeNetRequest` 剥头。
- Gemini key 在 URL query 会进中转日志（诊断已脱敏）；后续可改 `x-goog-api-key` 头。
- `tokenizeGlossary` 子串替换可能误伤大小写恰好组成的词（术语 "AI" 与 "SAID"），影响极小。
- 页面无可译内容时引擎 state 保持 off，工具条仍显示「翻译」（语义合理）。
- `acquireStartSlot` 令牌等待的 sleep 不检查会话 signal：Retry-After 冷却期间 abort 需等睡醒才生效（窗口极窄）。
- `toMicrosoftSourceLang` 缺 `mo` 变体（经归一不可达）。`translate.autoDetectSource` 曾是休眠设置，
  2026-09-26 已补齐消费：content/engine.ts `translateAll` 在其为 false 时跳过源语言检测（默认 true 行为不变）。
- 拖动/吸附未做仿射反解：html 自身带 transform/zoom 的站点拖动位移有缩放误差（此类站点极少，初始落位正确）。
- content 侧设置均为装载时快照：会话中途改设置（站点规则/自动翻译等）需刷新生效（历史语义一致）。
- pendingTexts 跨批串扰与共享文本统计粒度小偏差：下一轮翻译自愈，彻底解决需引用计数，收益不匹配。
- 划词 caret 几何（`caretRangeFromPoint`）jsdom 无法覆盖，hoverIframe 用例走旧 fallback，需真实浏览器验证。
- 行内链接拆段降级路径会牺牲该句链接可点击性（宁失可点击，不出重复/中英夹碎），换序频率待线上观察。

### 2026-09-18 全仓库审查遗留（4 个 P0 已修并变异自证 6/6，以下为未修部分，详见 git 历史 结论2.md / 最终结论.md 第四节）

- **P1 渲染可逆性组**：嵌套单元快照与还原过滤基准取自不同时刻（还原重复追加/内层原文丢失）；`data-it-src`
  是永久过滤标记且无文本校验（站点改写已译容器后新文本永不重译）；虚拟列表复用节点残留 `data-it-src`/`data-it-inside`
  （后者只写不清）致节点被永久排除；容器被站点搬走后还原会从新位置摘回旧包裹位置；`restore()` 不清理 shadow
  样式表/lastError/失连条目，`textTranslations` 无上限。
- **P1 状态语义组**：`suppressPageState` 吞掉 `done` 使 state 恒为 off → 悬停单译后点工具条触发**整页翻译**而非还原
  （语义反转，下轮动 engine 状态机时与 P0-2 同文件一并处理）；同根因使观察器在悬停单译期间停摆；
  ~~`isOwnNode` 把 `.it-orig`/`.it-wrap` 当自身标记~~（2026-09-20 已修：收紧为节点自身分层判定，见
  第八节 B3；白名单通配符仍未支持）；
  白名单不接受通配符且无提示（写 `*.example.com` 后全站静默停止翻译）。
- **P1 请求放大组**：abort 穿不透限速/429 冷却的 sleep（上限 5 分钟，已取消请求仍占并发槽与 keepAlive）；
  ~~批次级硬失败后仍对整批逐段重发（30 段 → 31 次 401）~~（已修：**不可重试类**鉴权/404/参数错误在整批与小批量
  两处 catch 直接短路上抛；429/5xx/网络属可重试，仍保留降级——小批量确实可能挤过限流）；~~段落级失败写成 `""`
  混入成功结果（无失败标记、stats.error 不增）~~（2026-09-20 已修：渲染层按单元空译文走 fail 角标并
  撤销 doneTexts，见第八节 B8；后台部分成功不再整批丢弃见 A2）；流式用总超时而非空闲超时（长流被中途砍断）；
  `splitBatch` 静默剥掉译文里本就合法的行首编号且写入缓存（实测复现）；
  ~~Edge TTS 外部请求无超时无取消、`tts-synthesize` 无 beginKeepAlive~~（已修：取令牌 10s / 合成 30s 硬超时，
  计时作用域覆盖到响应体读完，`tts-synthesize` 补上 begin/endKeepAlive。注：原记「三个请求」有误——
  `public/offscreen.js` 全程零 fetch，只做 atob + Blob 播放；真实缺口是 `edgeTts.ts` 的两个）。
- **P1 共享模块组**：`langDetect` ja/ko 无密度阈值（一个假名即判日语，实测复现）；
  ~~站点规则模式整体小写而 `url.pathname` 原样比对~~（已修：比对前解码百分号 + 小写，畸形转义退回原样）；
  ~~host 通配符拼正则不限 `*` 个数 → 灾难性回溯~~（已修：不再构造正则，改无回溯的两指针通配匹配 +
  通配符数上限 8。实测旧实现 6 组通配符 4.2 秒、**8 组 187 秒**，且跑在 content 主线程同步路径上，
  比原记的 3.2 秒严重两个数量级）；~~导入数值字段无范围校验~~（已修：全部数值项加区间校验，越界即当未提供丢弃。
  严重性也需更正：`blockMaxChars: 0` 不是「阻塞 2s 再抛错」，而是 `i += maxChars` 永不推进 → **主线程无限死循环**；
  且该字段与 `minTextLength`/`cache.maxEntries` 在设置页**没有输入控件**，导入文件是唯一入口）；
  `mergeSettings` 浅合并外泄 DEFAULT 数组引用（已随 P0-4 修复深拷贝）。
- **P2 全量清单**（siteRules 中段通配过度匹配、~~manifest 零使用的 `scripting`/`activeTab` 过度授权~~
  （已删，全仓零调用已核实）、~~发布 zip 含 sourcemap 且 `.map` 暴露在 `web_accessible_resources`~~
  （已修：`sourcemap: false`——`"hidden"` 不够，文件仍在 dist 且仍被 @crxjs 列进 WAR，用「加载已解压扩展」装
  dist/ 时泄露面不变；zip.mjs 另加 .map 排除 + dist 早于源码时拒绝打包；2026-09-20 进一步以 standalone
  IIFE 使 WAR 整段消失 + zip.mjs 改 fflate，见第八节 B4/C11-C13）、options/popup 的 async 监听器无
  try/catch（已修 3 处，余 7 处；2026-09-20 C14/C16 已全部补齐）、cache 内存层 FIFO 非 LRU 等 20+ 条：原文见 git 历史 结论2.md 附录。
- **P0-4 历史残留**：修复前以明文落盘过、且恰为合法 Base64 形态的 API Key 仍会显示为乱码，需用户在设置页
  重输一次（无法从存储侧无损判别历史明文，不做自动迁移）。

### 2026-09-18 晚 · 第四轮（4 路子代理并行审查 + 收口）

分维度派 4 个子代理（content 层 / background+shared / 测试质量 / 工程配置），要求逐条核实上面所有遗留项。
核实结论：**上轮归档的 30 项只真正修好 3 项**（content 的嵌套快照口径、容器搬走还原、pendingTexts 串扰），
background+shared 的 14 项一字未动。本轮已修部分见上文删除线。

本轮**新增**发现并已修的：

- **API Key 明文落盘**（`storage.ts`）：v1 混淆在 UTF-16 码元上 XOR 后 `btoa`，含非 Latin-1 字符（中文、
  带重音符的 Key）时 `btoa` 抛错 → `catch` 返回**明文原文**写盘。已改 UTF-8 字节编码（v2），任何字符集都
  不落明文；读取仍兼容 v1，下次保存自动升级。实测复现过：`at-v1:sk-密钥test` 原样进磁盘。
  留观：这套编码**不是加密**（固定常量密钥流、无秘密输入、可还原），函数名 `encryptApiKey` 与设置项
  `security.encryptApiKey` 均有误导性——但改名要动存储 schema，暂留，注释里已写明「不要指望本函数」。
  真要加密须引入不可导出密钥（`crypto.subtle` + `chrome.storage.session`）。
- **测试套件 2 个空转用例**：对 `src/` 做 11 次字符级变异自检，7 组如实变红，2 组「实现坏了 440 用例仍全绿」：
  缓存哈希碰撞用例的未命中其实来自「条目无 ts → 按 0 处理 → 立即过期」而非 `src` 校验（已补 `ts` + 反向对照
  断言，防「get 恒返回 undefined」蒙混）；术语表「长术语优先」断言的是 `⟦n⟧` 下标，而下标按**排序后**数组分配，
  反转排序后断言逐字成立（已改断言替换后的实际文本）。两条都已重新变异确认能变红。
- **`typecheck` 是假绿灯**：`tsconfig.json` 的 `include` 只有 `src`，46 个测试文件从不参与类型检查，
  纳入后立刻暴露 27 个文件、约 40 处真实类型错误（含 `as Settings` 硬转掩盖 fixture 缺字段）。已全部修掉，
  并按「src 保持 `types: ["chrome"]`、测试另用 `tsconfig.test.json` 开 node 类型」拆分——避免 content script
  误用 `process`/`Buffer` 也能过编译。`eslint` 0 问题也需打折看：`eslint.config.mjs` 未 extends 任何
  recommended 且显式关掉 `no-explicit-any`，属「没在查」而非「没错误」。
- **旧测试把 P0 钉成契约**：`providerRequests.test.ts` 原有一条断言「整批 401 → 逐段降级共 21 次请求」，
  即把请求放大行为当成正确语义锁死了。修短路时同步改成断言 1 次，并加了 URL 不得含 Key 的反泄密断言。

本轮**仍未修**（维持留观）：上文的 abort 穿透 sleep、流式总超时、`splitBatch` 剥行首编号、
`langDetect` ja/ko 密度阈值、`suppressPageState` 吞 `done`、白名单不支持通配符、
拖动仿射反解、content 设置为装载快照、`textTranslations` 无上限、`restore()` 不清 `lastError`、
属性翻译 `data-it-attr-done` 元素级标记致多属性只译其一、`addedRootsSeen` 溢出失同步、
`retryPendingSplits` 每次 fill 全量重算、cache FIFO 非 LRU。
（2026-09-20 已修其中五项：`isOwnNode` 误判 → B3；段落级失败写 `""` → B8；async 监听器 → C14/C16；
`data-it-src` 永久过滤标记 → B3 借用标记方案；零覆盖盲区 → D8 两文件。）

覆盖盲区（2026-09-20 D8 已补两处：background/index.ts 消息路由黑盒 21 用例、批量缓存命中 true 侧 +
部分命中侧；其余维持盲区）：`src/content/scheduler.ts` **零测试**（整页调度核心）、
`src/background/translate.ts` 的降级放大/429 退避/摘要缓存无独立用例（B1/A2/C8 修复已在
p0Regression 补关键路径）、`googlefree.ts` 零直接测试（providerRequests 有间接批量协议覆盖）。

验证状态：`npm test` 46 文件 **455 用例全绿**（含真实浏览器 e2eSmoke 4 例，删权限未打破运行时）、
`npm run typecheck`（双 project）0 错误、`npm run lint` 0 问题。
`npm run format:check` 不过，但**是既有债务**：同一 prettier 3.9.6 下 HEAD 基线就有 98 个文件不合规，
当前为 56，且 CI 不卡这一项，未做全仓重排以免制造噪音 diff。

### 2026-09-18 深夜 · 第五轮（全量 diff 复审 + 4 项修复）

第四轮改动入库前做了一轮人工全量复审（推演了 wildcardMatch 无回溯算法、v2/v1/明文三态判别、
401 短路与备用切换的交互），确认第四轮核心修复全部成立，无 P0/P1。本轮修掉复审发现的 4 项：

- **UI 保存路径与导入校验区间不一致**：第四轮给导入加了区间校验（如 `maxConcurrency: numOf(1, 10)`），
  但设置页保存仍只钳下限（并发可输 100）、超时完全无范围（可输 1 秒，低于导入下限 5 秒）——同字段两套规则。
  已把全部区间收敛为 `storage.ts` 导出的 **`SETTING_RANGES` 单一来源**，导入校验与 UI 保存钳制共用，
  UI 侧超时按「秒输入 → 落盘毫秒」用 `timeoutSeconds` 衍生区间。`p0Regression.test.ts` 加了区间契约用例
  （防有人只改一处，含「秒区间 × 1000 = 毫秒区间」的一致性断言）。
- **Edge TTS 令牌失效重发共用计时窗口**：`synthOnce` 原把首次请求与 401 后重发放在同一个
  `withRequestTimeout(30s)` 作用域——首次请求耗掉大半窗口后返回 401，重发只剩零头时间，
  一次可恢复的令牌失效被误报成超时。已改为重发**独立起算 30s**。顺带修了一个次生问题：
  `withRequestTimeout` 原来在 `signal.aborted` 时一律报超时，会吞掉同刻发生的业务错误
  （如响应格式无效）；现在只在收到 `AbortError`（fetch 对 abort 的真实反应）时才替换成超时文案。
  新增 3 个用例：挂起请求 30s 被打断（fake timers，注意 crypto.subtle 异步链在时钟推进中途
  才注册超时器，须分段推进）、HTTP 500 不被超时文案吞掉、401 前置等待 3s 后重发仍全窗口可用。
- **路径模式侧不解码百分号转义**：第四轮给 URL 侧加了 `pathnameForMatch`（解码 + 小写），
  但模式侧没解码——用户从地址栏复制 `a.com/%E6%96%87%E6%A1%A3` 这类编码模式永不命中
  （地址栏通常显示解码形态，此类输入真实存在）。两侧现已同口径解码比对；畸形转义（`%zz`）
  两侧都退回原样按字面比对。
- **zip.mjs mtime 检查的误报来源**：`git checkout`/切分支会把源码 mtime 刷新为当前时间，
  之后即使 dist 仍有效也会被「dist 早于源码」拦下。属保守方向的误报（多跑一次 build 无害），
  不改判定逻辑，只在报错信息里写明原因与处理方式，避免未来被当真 bug 排查。

本轮验证：`npm test` 46 文件 **459 用例全绿**（净增 4 例）、`npm run typecheck`（双 project）0 错误、
`npm run lint` 0 问题。复审确认无误、维持留观的：`withRequestTimeout` 对「业务错误与 abort 同刻发生」
的归属仍按超时外的原始错误处理（现有语义可接受）；package-lock 净删 512 行是 npm 本机重生成清掉了
esbuild 其他平台 optional 二进制（其他平台协作者 install 会添回），提交时已说明。

### 2026-09-20 · 第六至八轮三报告交叉验证收口（2 P0 + 5 P1 + 6 P2 修复）

三份独立审查报告（第六轮 hy4 / 第七轮 ds / 独立复审 glm，原文见 git 历史）经 7 个只读子代理分批
逐条核对代码与基线命令后交叉仲裁（约 90 条结论：绝大多数成立、15 条更正、2 条推翻），按终裁顺序
由 9 个修复子代理分 3 批（每批 ≤3，按文件无冲突分派）落地。**全部修复项**：

- **A1 响应体读取收回超时/中止作用域（P0）**：`http.ts` postJson / `googlefree.ts` / `microsoft.ts`
  三处非流式请求的 `await res.text()` 原来都写在 try/finally 之外——body 悬挂时 promise 永不 settle，
  连锁占死 runConcurrent 并发槽、keepAlive 永久 poke、`index.ts` 会话控制器永久留存、等待队列饿死整条
  共享限并发管线（含其他标签页与页面摘要）。三处统一照 `postBodyStream` 范本重构：读体移入计时/中止
  窗口，finally 统一清理（googlefree 的 releaseSlot 同步移入）。**keepAlive 加 30 分钟硬上限**纵深防御
  （超限自动停心跳并 warn 一次，正常提前结束撤掉计时器，导出 API 不变）。
- **A2 部分成功的整批被整体丢弃（P0）**：`translate.ts` 尾部判定原基于 `pending`，逐段兜底全失败时
  `every` 恒真 → 已成功段落一并作废。改为 `failedSegments` 集合 + 按 `toFetch` 全集判定：**成功段数 === 0
  才整批 throw**，部分成功保留译文（失败段维持 `""`）；顺带收敛了 12 次失败请求的放大（小批量 2 段失败
  → 4+8=12 次）。新增变异自检用例钉住「3 段中 1 段持续失败 → 保留 2 段译文不整批抛」。
- **B1 硬失败时备用/免费互切永不启用（P1）**：通道切换门从 `isRetryable` 改为「还有下一个候选就切」
  （主 401/400/bad_response 等硬失败现在会切免费互切与备用，全候选失败抛最后一个错误并标注来源）；
  通道内级联短路保留（30 段 401 仍只发 1 次）。**D3 连带**：整批/子批 catch 不再裸 throw 逃逸——硬失败
  把剩余段标记失败后走统一收尾（取消归一化、allFailed 聚合），子批硬失败不再中断兄弟组。
- **B2 body 读取失败被吞成空串（P1）**：主路径 `res.text()` reject 原被 `.catch(() => "")` 吞掉，
  JSON.parse("") 再误报「BaseURL 配置错误」。现在读体 reject 按 network 分类（可重试可切备用），
  超时中止归 timeout、会话中止归 cancelled；`!res.ok` 错误体保留空串兜底（仅诊断预览用）。
- **B3 观察器两条结构性漏扫（P1）**：`onMutations` 收集第一条外部记录即 `return` → 改为全量收集后统一
  调度；`isOwnNode` 从 closest 爬祖先收紧为**节点自身判定**（分层：直接标记 / `data-it-src`、
  `data-it-processing` 等借用标记 / 文本节点看父元素快照标记 / UI 子树有界攀爬），去掉把外部节点永久
  缓存为自身产物的 `ownNodeCache`（UI 子树攀爬保留安全的 `ownUiCache`）。核对全部自产 DOM 写入点
  确认无自激回归，10 个新用例含「外部节点被产物包裹仍判外部」的误判回归点。
- **B4 Key 混淆代码暴露给任意网页（P1）**：@crxjs 对普通 module 内容脚本硬编码
  `use_dynamic_url: false`，无选项可改；改用插件的 `contentScripts.standaloneFiles` 把
  `src/content/index.ts` 打成自包含 IIFE（内容代码零动态 import、零 import.meta，已核实）——
  构建产物 `web_accessible_resources` **整段消失**，storage/langDetect/i18n 等 chunk 不再被任何网页
  fetch 到（仅扩展自身页面静态引用）。
- **B5 CI 上的 e2e 永久绿灯空转（P1）**：build.yml 把 Build 提到 Test 之前 + 补
  `playwright install --with-deps chromium`，e2e 在 CI 可真实运行；4 处「裸 return 记 pass」守卫改
  `it.skipIf`（跳过在报告中可见，SKIP_E2E 语义保留，launch 失败 warn 原因）。顺带落 CI 卫生项：
  concurrency、timeout-minutes: 30、permissions 收紧、artifact retention-days + if-no-files-found。
- **B6 shadow DOM 在途标记永久残留（P1）**：`clearProcessingMarks` 与 `restoreElement` 的标记清理
  复用 extractor 导出的 `collectShadowRoots` 穿 open shadow（顺带修复其对「root 自身是宿主」不递归
  嵌套 shadow 的既有缺口）。
- **B7 译文未渲染却记完成（P1）**：`renderer.fill` 改返回 boolean（真实写入才 true），renderBatch 按
  返回值记账，false（容器已 detached）按失败计并从 doneTexts 除名；fetchBatch 的 allRevoked 判定追加
  `!container.isConnected` 结构性条件——「容器被移除」不再骗过令牌比对，同文本不会被永久跳过。
- **B8 部分段空译文渲染成空白但成功（P1）**：renderBatch 按文本判定全部 chunk 空白 → 走 fail 失败
  角标、不计成功、撤销 doneTexts（该文本可重试）；整批全空短路保留。
- **B9 译文等于原文时段落被截断（P1）**：`applyTextNodes` 渲染前拼接快照原文比较，译文与之相等（含
  trim 口径）时逐节点原样保留——免译哨兵回显不再清空其余文本节点造成可见截断。
- **C6 backupApi 空对象/数组被接受（P2）**：导入归一化收紧——`{}` 与数组视为未配置（免费互切不再被
  静默禁用），显式部分字段仍按「缺项拿主 API 补」语义；3 个新用例钉住。
- **C8 429「容量降到 1」被下一次翻译抹掉（P2）**：`configureChannels` 跳过 `pausedUntil` 未到期的
  通道（暂停期内保持 capacity=1，到期后自然恢复）；rateLimiter 本体未动。
- **C11/C12/C13 zip.mjs 三缺陷一并根治（P2）**：改用 fflate（devDependencies）纯 JS 压缩——条目键
  强制 posix 正斜杠（跨平台产物一致）、版本号正则校验（`1.2.3'--evil` 实测拒绝、退出码 1，注入面
  根除，不再拼接任何 shell 命令）、.map 排除强化为大小写不敏感。产物经 unzipSync 读回 18 条目全量
  字节比对通过。
- **C14 测试按钮复用 readForm 无保护（P2）+ C16 余量**：options 的 btn-test / btn-test-backup 包
  try/catch/finally（readForm 抛「站点规则第 N 行无效」现在有可见反馈、按钮复原）；btn-clear-cache /
  btn-cleanup-cache / btn-export 同模式补齐（C16 清零）；popup 的 p-save / enabledToggle / addSite
  补 catch（失败给状态栏反馈，开关失败回滚到存储实际状态）。未新增 i18n 键。
- **C19 缺 minimum_chrome_version（P2）**：manifest 补 `"minimum_chrome_version": "109"`。影响表述
  按验证更正：109–115 上 TTS 因 `getContexts` typeof 守卫本就可用；真正坏的是 Chrome <109
  （`chrome.offscreen` 不存在直接 TypeError）。
- **D8 两处零回归保护补变异自检用例**：新增 `tests/backgroundRouter.test.ts`（21 用例，background
  路由黑盒：translate/缓存/摘要/TTS/取消等全部消息类型的回包形状、keepAlive 与会话注册成对性，
  变异验证：路由串改一个字母 → 红）；新增 `tests/batchCacheHit.test.ts`（3 用例：全命中零 fetch、
  部分命中仅未命中段合并一次请求、关闭缓存仍请求的对照侧；探针验证：缓存命中短路失效 → 红）。

**基线修正说明**：第六至八轮报告所指「459 全绿」在本机为真但在 CI 上为假（e2e 空转）——B5 修复后
CI 的 e2e 为真实运行或显式 skipped，测试数字不再打折。

**本轮未修（维持留观，多为 P3/设计权衡）**：abort 穿不透限速/429 冷却 sleep、流式总超时（非空闲
超时）、`splitBatch` 剥行首编号写缓存、`langDetect` ja/ko 密度阈值、`suppressPageState` 吞 `done`
（悬停单译后工具条语义反转）、白名单不支持通配符、拖动仿射反解、content 设置装载快照、
`textTranslations` 无上限、`restore()` 不清 lastError/shadow 样式表、`data-it-attr-done` 元素级冻结、
`addedRootsSeen` 溢出失同步、`retryPendingSplits` 全量重算、cache FIFO 非 LRU、C1 在途调用沿用旧
并发度、C2 设置失效押单个 onChanged、C4 属性翻译无在途保护、C5 lazyUnits 只增不减、C7 cacheVariant
在途切换键空间污染、C9 单次 429 冻结通道 5 分钟、C10 TTS 熔断计永久性错误、C21 依赖混源、C22 免费
通道残留 BaseURL/Key、C23 缓存统计全量读盘、D2-D21 各 P3 条目（详见 git 历史三份报告与 结论.md）。

本轮验证：`npm test` **49 文件 / 516 用例全绿**（净增 3 文件 57 例：observerScan 10、backgroundRouter
21、batchCacheHit 3 + 各回归文件新增 23 例）、`npm run build` 成功（dist manifest 无 WAR、
minimum_chrome_version 109、content 单文件 IIFE）、`npm run typecheck`（双 project）0 错误、
`npm run lint` 0 问题、`node scripts/zip.mjs` fflate 链路实测通过。

### 2026-09-26 · 第九轮（10 路子代理并行全量审查 + 逐条验证收口）

按模块分派 10 个只读审查子代理（content 流水线 / content 渲染层 / content 交互组件 / background
翻译服务 / provider 适配层 / TTS 链路 / shared 公共库 / options 与 popup / 构建与 CI / 测试质量），
发现按「简单 / 中等 / 难题」分级，主线对每条发现独立核实后再派修复子代理，难题由主线亲自处理。
构建与 CI 报告先行返回，3 项发现全部核实为真，已修 3 项：

- **vite 5 / vitest 4 版本分裂（中等）**：顶层 vite 5.4.21 与 vitest 4.1.10（peer 要求
  ^6||^7||^8）并存，npm 把 vite 8.2.0 嵌套装进 `node_modules/vitest/node_modules/vite`——仓库同时
  维护两条 vite 大版本线，安全补丁要跟两次，`vitest.config.ts` 与 `vite.config.ts` 分别由 vite 8 与
  vite 5 处理存在行为漂移。已升级顶层 vite 至 7.3.6（@crxjs 2.7.1 peer 支持 ^3~^8），lockfile 嵌套
  副本消失（净减 568 行）。全量回归实测：typecheck 双 project 0 错误、lint 0 问题（新
  --max-warnings=0 口径）、build 成功且产物特征保持（content 仍为自包含 IIFE、零 .map、manifest
  完整）、`npm test` 516/516 全绿（首轮 ttsSynthesize 一例在并发负载下 flaky，单文件重跑与全量重跑
  均过，与升级无关）。注意：npm 安装时 esbuild 0.28.2 postinstall 有安全策略提示，本机构建测试均
  正常，CI 若有同类策略需将 esbuild 加入 allowScripts 清单。
- **eslint 规则盲区（简单）**：`no-explicit-any` 显式 off、`no-unused-vars` 仅 warn——lint 无法拦住
  类型安全回归。两规则升为 error（当前 src/ 与 tests/ 零 `any`、零警告，升级零成本），lint 脚本加
  `--max-warnings=0` 双保险；`no-non-null-assertion` 维持 off（全仓仅 engine.ts 一处，改动风险
  大于收益）。`npm run lint` 实测 0 错误 0 警告。
- **zip.mjs 版本号单边来源（简单）**：zip 文件名版本只取 dist/manifest.json，package.json 不参与
  校验，单边改版号时 zip 以错误版本命名发布。已加两边 version 交叉校验，不一致直接拒绝打包
  （临时改 package.json 为 1.0.1 实测拦截生效，还原后恢复正常打包）。

第九轮其余模块报告返回后逐条核实，修复与留观项按上述格式续记于后。

**难题 1：options/popup UI 测试基建（主线完成）**：两页合计 727 行此前零测试，底层校验（importSettings/SETTING_RANGES/消息路由）有覆盖但 UI 粘合层（保存是否真的走钳制、popup 发的消息形状是否与路由匹配）无保障。新建：
- `tests/helpers/chromeMock.ts`：共享安装/卸载助手（`installChromeMock`/`uninstallChromeMock`），统一 chrome.storage 内存模拟与 onChanged/runtime/tabs/i18n 桩，强制 afterEach 清理——直接回应测试质量报告指出的「26 文件装 mock 不清理、worker 复用 globalThis 致隐性假绿」根因，也为后续全量改造铺路。
- `tests/optionsUi.test.ts`（3 例）：越界超时值保存被钳制到 300s、导入非法 JSON 状态栏报「导入失败」且不破坏现有设置、导出成功有状态提示。
- `tests/popupUi.test.ts`（3 例）：总开关切换写入 storage.enabled 并同步 UI、「加入白名单」落盘 sites.whitelist、「加入黑名单」落盘且从对立名单移除。
基建关键点：jsdom 需补 `CSS.escape` polyfill（options/main.ts:418 依赖）；`vi.resetModules()` 强制 main.ts 顶层 init() 每用例重新求值（否则三例共享模块缓存、监听器绑在旧 DOM 上）；jsdom 对 scrollTo/navigation 的 "Not implemented" 日志为无害环境噪音。6 例全部真实驱动 UI 并断言 storage 落盘，单文件运行 291/372ms 全绿。

**测试质量修复批 B：观察器测试固定睡眠改条件轮询（已完成）**：dynamicMenu.test.ts 与 dedupeScheduling.test.ts 的 `waitObserver()` 原固定睡 550ms 再断言——防抖 300ms + 翻译往返被 CI 拖过 550ms 即假失败，且只证明"550ms 内完成了"。改造：正向用例改用 `waitFor(终态条件, 5s)` 轮询真正要断言的终态（如译文文本出现）；负向用例（"之后不应再有新请求"）改用 `awaitQuietPeriod()`——先真实跨过防抖窗口让 run() 有机会执行，再轮询 sendMessage 调用数连续 150ms 无变化（在途异步全部落账）才放行，时长自适应负载。两文件 8 用例两轮运行全绿，无状态泄漏。

**shared 修复批 C：IDN 域名与幽灵字段（已完成，35/35 全绿）**：
- **IDN 域名规则静默失效（简单）**：`normalizeUrlPattern` 的 host 白名单 `/^[a-z0-9.*-]+$/` 直接拒绝非 ASCII，用户写 `豆瓣.com` 返回 null 规则永不命中且无告警。已加 `toAsciiHost()`：host 含非 ASCII 时借用 `new URL("http://" + host).hostname` 转 punycode（与浏览器对页面 hostname 的归一口径一致），通配符 `*` 按段拆分逐个转换再拼回（`*.豆瓣.com` → `*.xn--klyv21c.com`），非法 IDN 返回 null。端口检查前移为独立一行（有意设计不变）。siteRules.test.ts 新增 1 例（含 `normalizeUrlPattern("豆瓣.com") === "xn--klyv21c.com"` 断言、带协议头、通配符+IDN、端口拒绝、非法 IDN 拒绝、端到端命中）。
- **两个幽灵设置项补齐实现（简单）**：`translate.autoDetectSource` 与 `security.encryptApiKey` 全仓零消费却被默认值/导入校验/迁移逻辑郑重维护。选"补齐实现"而非删字段（避开存储 schema 迁移风险）：a) autoDetectSource 在 engine.ts 真正生效——false 时跳过 detectPageSourceLang，源语言置空（下游空值保护已逐一确认：withSourceLangContext 空串不注入、googlefree/microsoft 空 sl/from 走端点自动检测），默认 true 行为不变，新增 autoDetectSource.test.ts 2 例；b) encryptApiKey 在 storage.ts 真正可控——false 时 saveSettings 跳过 encryptApiKey 明文落盘，读取侧 decryptApiKey 对明文幂等（非 Base64 原样返回、合法 Base64 但不匹配 v1/v2 前缀也原样返回），storage.test.ts 新增 3 例。两字段在 types.ts 补注释指明消费位置。

**测试质量修复批 A：测试简单项 6 项（已完成，51/51 全绿，变异自检 2/2）**：
- **e2eSmoke 永真断言**：`expect(true).toBe(true)` 改为真实结构断言——page.evaluate 检查 `#para` 位于 `.it-wrap` 内、存在可见的 `.it-translated.it-done` 兄弟节点且文本含 mock 译文后缀。waitForFunction 只证明"无隐藏译文+原文开头是英文"，若切回双语时译文元素被误删照样满足；新断言补上该盲区。
- **settingsCompat 弱断言**：`expRaw`（恒等于 exported 且从未消费的死代码）改为真实断言——导出体内 apiKey 为字符串、不等于明文、且能被 decryptApiKey 解回原文（密文形态损坏会让导入路径解出乱码，not.toContain 抓不住）。
- **SO fixture 兄弟选择器写错**：原 fixture 把 `div.s-prose.js-post-body + div` 当 class 字面量，且坏 div 前还隔着签名块即使合法结构也不命中。重排回答区结构为真实兄弟关系，siteRulesFixtures.test.ts 新增 1 例（提示条被排除、正文段落仍提取）。
- **ttsSynthesize 真实睡眠转 fake timers**：「独立计时窗口」用例两次 3s 真实 sleep 改为 fake timers 分段推进（crypto.subtle 签名链真实异步须分段推），整文件从 6s+ 降到 37ms。
- **补「令牌临近过期刷新」用例**：makeToken 增加 ttlSec 参数，新用例返回 exp=now+60s 的令牌，断言第二次合成时令牌端点被再次请求且两次 Authorization 不同。变异自检：把刷新条件改成 `now < expiredAt`（只在真过期后刷新），恰好只有本用例变红。
- **补「熔断到期自动恢复」用例**：vi.useFakeTimers 推进系统时钟，5 次失败开路后推到 15min-1ms 仍开路、推到整点边界熔断关闭、恢复后重新发请求。变异自检：`>` 改成 `>=`，恰好只有本用例变红。

**options/popup 修复批 E：6 项（已完成，32/32 全绿）**：
- **[中等] options 陈旧表单覆盖 popup 操作**：保存是全表单提交，sites 段来自页面打开时的 textarea，popup 期间加的白/黑名单被静默回滚。修复：loadForm 时记录 initialSiteInputs 快照，readForm 的 sites 段比对当前值与快照——相同（未编辑）则沿用 current.sites 现值，不同才解析表单值；保存成功后也刷新快照封住连续两次保存之间的窗口。optionsUi.test.ts 新增 2 例（未编辑不回滚 popup 白名单、已编辑仍生效）。
- **[中等] test-connection 缺 keepAlive**：慢端点（首字延迟超 30s）测试时 SW 被回收，sendResponse 通道消失误报失败。修复：test-connection 分支加 beginKeepAlive/endKeepAlive，与 translate/page-summary/tts-synthesize 完全同构。backgroundRouter.test.ts 补成对断言。
- **[简单] 备用通道切免费清空配置**：备用通道切 googlefree 保存时主动写空串（backupFree ? "" :），与主通道「禁用但保留」语义不一致。修复：去掉三个三元直接落输入框现值（googlefree provider 本就忽略这些字段）。optionsUi.test.ts 新增 1 例。
- **[简单] 测试按钮双击双发**：btn-test 先 await readForm 才禁用，窗口内第二次点击触发完整处理器。修复：处理器第一行同步 btn.disabled=true（任何 await 之前），finally 恢复。options 的 btn-test/btn-test-backup 与 popup 的 p-test 三处同构修改。optionsUi.test.ts 新增 1 例。
- **[简单] 站点规则 JSON 错误报成「测试失败」**：用户在站点面板粘贴 JSON 有笔误的规则后只想测 API 连通性，btn-test 先 await readForm 触发 parseSiteRulesTextarea 抛错报成「测试失败」且阻断测试。修复：readForm 的 API 段抽为 buildApiForm/buildBackupApiForm（保存与测试共用防漂移），新增 readApiForm 只读 API 段不触发站点解析，btn-test/btn-test-backup 改走 readApiForm。optionsUi.test.ts 新增 1 例。
- **[简单] 窄屏隐藏状态栏**：≤860px 时 .status display:none，保存失败/导入错误/测试结果全部不可见。修复：改 display:block + 单行截断（flex: 1 1 auto; overflow: hidden; text-overflow: ellipsis）。

**content 交互修复批 F：6 项（已完成，148 相关测试全绿）**：
- **[简单] input 按钮缺 data-it-ui 标记**：按钮会被 extractor 当页面控件翻译（"译" 2 字符通过 shouldTranslate），title 也被属性翻译。修复：showButton 创建按钮时补 `b.setAttribute("data-it-ui", "")`（与 bubble/hover 一致）。
- **[中等] React value tracker 未绕过**：setFieldText 直接 el.value=text 走 React 实例 setter，tracker 的 lastValue 同步为新值，随后 input 事件被 React ChangeEventPlugin 判定"值未变化"吞掉，onChange 不触发。修复：input/textarea 分支改用原型 setter（`Object.getOwnPropertyDescriptor(HTMLInputElement.prototype, "value").set.call(el, text)`）绕开实例 tracker 再派发 input 事件；contenteditable 分支 textContent 赋值后补派发 InputEvent。
- **[简单] 气泡"先点按钮再翻译"分支未先 close()**：translateOnSelect=false 时旧流式气泡被 bubble=btn 覆盖引用，脱离一切关闭路径永久滞留。修复：else 分支开头调用 close()（mousedown 已提前关闭时重复调用幂等 no-op）。
- **[中等] 总开关关闭不关气泡/不中止在途流**：onOff 只 restoreForSwitchOff，气泡 close 封闭在 initBubble 闭包内无外部入口。修复：initBubble/initInput 返回清理函数（close/destroy，input 的 destroy 含 inFlightAbort 闭包标志中止在途翻译），index.ts 的 onOff 统一调用；input 的 onButtonClick 在 await 返回后补 isBlocked 检查再回填。
- **[简单] body translate=no 失效**：isExcluded 爬链循环条件 el !== document.body，body/html 自身的 translate="no"/aria-hidden/hidden 不参与判定。修复：爬到 body 后补判 body/documentElement 两级的这三项（不复用 isSelfExcluded 避免 usesIconFont(body) 误判全页），结果缓存到 excludedCache（每次提取新建 Map，天然"本次提取常量"）。
- **[简单] 工具条位置记忆直写 left/top 绕过仿射反解**：restoreState 直写坐标，跨页面恢复到 html 带 transform/zoom 的页面时按失真坐标系解释。修复：restoreState 改调 placeFixedInViewport（placement.ts 的模块级导出，两点采样反解），与初始定位/拖拽落点同路径。

**测试质量修复批 I：backgroundRouter 生命周期补测（已完成，25/25 全绿）**：chrome 桩的 onInstalled/onStartup/onAlarm/commands.onCommand 原只注册不执行，background/index.ts:42-73 三段逻辑（onInstalled 建清理 alarm、onAlarm 调 cleanupCache、onCommand 中继 it-command）零覆盖。修复：addListener 改命名 spy，beforeAll 从 mock.calls 取出监听器直接调用驱动，补 4 例——onInstalled 建 alarm（名字 it-cache-cleanup、周期 24h、延迟 1min 精确匹配）、onAlarm 正确名调 cleanupCache、错误名忽略、onCommand 查询活动标签页并中继 it-command（tabsQuery 参数与 tabsSendMessage 载荷字段名与值双钉）。

**测试质量修复批 G：chrome mock 清理 28 文件（已完成，52/52 文件全绿）**：37 个测试文件安装 chrome mock 只有 7 个清理，Vitest worker 跨文件复用 globalThis 致残留 mock 造成"单跑绿、全跑红/绿"的隐性假绿。修复：18 个文件迁移到共享助手 installChromeMock（），10 个保留自定义 mock 但补 uninstallChromeMock 清理（autoTranslatePerSite/cacheSettings/providerRequests/settingsCompat/pageSummary/batchCacheHit/masterSwitch/microsoftProvider/requestDedup/storage）。迁移判定经两处预检：get 空值语义（共享助手对未存键返回 {key: undefined} 与旧 mock 的 {} 在全部消费方的属性访问模式下语义等价）、保留组特征（带 storage.local.remove 或外部可控 spy 驱动模式的强迁会改测试语义）。全仓库 52 个测试文件逐个单跑 PASS。

**测试质量修复批 K：cachedCount 高命中分支补测（已完成，4/4 全绿）**：engine.ts:302 的 checkPageCacheRatio「命中率高 → 跳过懒观察、整页直译」关键分支零覆盖（所有内容侧测试的 sendMessage mock 都返回 cachedCount: 0）。修复：lazyViewport.test.ts 补一例——check-cache 返回 cachedCount=3（命中率 1.0 >= 0.6 阈值），断言不经 IntersectionObserver 即全部 3 条文本出译文（observedElements.size === 0、ioCallback === null），与现有 3 个懒观察用例（cachedCount: 0 时屏外单元只进 observer 零翻译请求）形成对照。checkPageCacheRatio 判定逻辑：去重后抽样前 40 条、阈值 >= 0.6、forceFull 时直接 translateUnits 整页直译否则才走 observeLazy。

**难题 2a：e2e CI skip 可见性（已完成，e2e 4/4 全绿，typecheck 0 错误）**：canRunE2e() 任一条件不满足就 skip，CI 报告里 4 个 skipped 与全绿混在一起无法察觉。修复（方案 B：断言模式环境变量）：canRunE2e 新增 guardReasons() 逐条计算不满足的条件（SKIP_E2E 设置/chromium 探测失败/dist 缺失），CI_ASSERT_E2E=1 时条件不满足直接 throw（消息逐条列出原因并提示排查上游步骤），未设该变量时行为与原来完全一致。CI 在 npm test 后加一步 `CI_ASSERT_E2E=1 npx vitest run tests/e2eSmoke.test.ts`，保证 e2e 在 CI 上要么真实运行要么显式红灯。方案选择理由：失败消息来自代码本身（方案 A 解析 JSON 无法说明为什么）、不依赖 vitest reporter 的 JSON schema（vitest 4 结构随版本变动）、exit code 天然让 workflow 变红、成本更低（只重跑单文件约 5.6s）。本地验证三种场景：有 dist+Chromium 时 4 passed、模拟 Chromium 缺失时 FAIL exit=1 带清晰消息、SKIP_E2E=1 时 FAIL 首条为「SKIP_E2E=1 已设置」不误报探测失败。顺带修复 optionsUi.test.ts:136 的 TS2353（backupApi 类型缺 apiKey 字段）。

**TTS 修复批 H：3 项（已完成，83/83 全绿）**：
- **[简单] crypto.randomUUID 在 http 页面失效**：manifest 匹配 http://*/*，非安全上下文中 crypto.randomUUID 抛 TypeError，http 页面朗读 100% 失败且浪费一次合成。修复：requestId 改用与 id 字段同款的自增方案（tts-r-${Date.now()}-${++msgSeq}），requestId 仅需标识一次播放无需 UUID 语义。
- **[中等] stop/play 时序洞（孤儿播放）**：ttsPlay 在 await ensureOffscreenDocument() 后才 sendMessage，用户在此窗口点停止 → it-tts-stop 先到 offscreen 当 no-op（current 还是 null），随后 it-tts-play 落地成孤儿播放。修复：模块级停止代际 stopSeq，ttsStop 入口递增，ttsPlay 进入时快照 myStop，在正常路径与重试路径发送前校验 stopSeq !== myStop 命中则返回 { finished: false }。关键细节：beginKeepAlive 挪到 try 之前、门禁提前返回放入 try 内，否则提前返回会绕过 begin 却执行 finally 的 endKeepAlive 错误递减其他在途翻译的保活引用计数（子代理首版引入的缺陷，已补配对回归用例钉死）。
- **[中等] 页面导航/关闭标签页不停朗读**：播放中跳转页面或关闭标签页，content script 随页面销毁，没有任何 unload 钩子发 tts-stop，offscreen 音频继续播到自然结束。修复（Port 模式）：content 播放开始建立存活信号 Port（chrome.runtime.connect({ name: "tts-play:<requestId>" })），收尾全部 disconnectLiveness 撤销；background 的 onConnect 增加 tts-play: 前缀分支，onDisconnect → handleTtsLivenessDisconnect(requestId) 校验断开的 requestId 是否仍是登记中的 activePlayId，命中才清登记、递增 stopSeq 并调 ttsStop()。Port 随帧销毁自动断开不依赖 content 存活，可靠覆盖跳转/关标签页/iframe 销毁；requestId 比对防止旧播放被新播放打断后旧 Port 迟到断开误杀新播放。ttsPlayback.test.ts +5 例、ttsController.test.ts +3 例、backgroundRouter.test.ts +2 例。

**shared 修复批 D：storage 互斥队列 + langDetect 密度门槛（已完成，42/42 全绿）**：
- **[中等] settings 整对象读-改-写无互斥**：popup/options/content 各自 getSettings()→改→saveSettings()，多 context 并发互相覆盖丢更新。修复：storage.ts 新增唯一入口 updateSettings(mutator)，模块级 settingsQueue 把同键写操作串行化（getSettings 读最新 → structuredClone 深拷贝为 draft → await mutator(draft) → saveSettings(draft)），mutator 抛错时该次不落盘但队列不饿死。调用点：popup 总开关（mutator 只改 enabled，快速连点的写序颠倒根治）、popup addSite（原子完成「加入本名单+从对立名单移除」）、content perSite 的 it-disabled-pages（独立 per-key 队列 disabledQueue，以存储最新值为底读-改-写）。options 保存维持 saveSettings（并发会话已实现快照脏检查，与 updateSettings 队列互补）。storage.test.ts +4 例（两个并发 updateSettings 交错、mutator 抛错、深拷贝、返回值为最终落盘）。
- **[中等] langDetect ja/ko 无密度门槛**：中文页含 1 个假名即整页判 ja，污染 googlefree 的 sl=ja 和 LLM 系统提示词。修复：zh/ja/ko 同式密度门槛（count >= max(5, len*0.15)），ja 额外加假名/汉字占比判断（kana < kanji * 0.1 → continue 落 zh，中文夹带外来词场景），zh 分支修正为按相对密度（假名数 >= len*0.15）才让位（解决与新 ja 回落机制的冲突）。langDetect.test.ts +4 例（80+ 字中文含「フィギュア」判 zh、纯日文假名/汉字比 52:16 仍判 ja、韩文过门槛判 ko、较长中文夹单个假名专名落 zh）。

**provider 修复批 L：6 项（已完成，70/70 全绿）**：
- **[简单] googlefree acquireSlot 超卖**：`if` 改 `while (activeRequests >= GOOGLE_CONCURRENCY)`，被唤醒后复检——releaseSlot 唤醒 waiter 与新请求抢槽之间存在 microtask 间隙，若不复检同一槽位会被同时发给被唤醒者与新到来者（在途并发突破 3 上限，放大触发 Google 限流概率）。
- **[简单] withStreamFallback 不区分错误类型**：429/401 也立即回退非流式重打一次（429 在退避生效前多打一发冲击已限流端点、401 把硬失败请求量翻倍）。修复：仅 not_found/bad_request/bad_response/network/timeout（疑似端点不支持流式或网络层失败）才回退，rate_limit/auth 直接上抛交上层重试/切换逻辑。
- **[简单] openai 流内 error.message 未脱敏**：中转站在 200 SSE 流内回 `{"error":{"message":"Incorrect API key sk-xxx"}}`，message 未经 redactSecrets 直接拼进 ApiError.message，密钥随错误消息到达 UI。修复：拼接前过 `redactSecrets(message, url, { Authorization: "Bearer ..." })`。
- **[简单] anthropic 流内 error.message 未脱敏**：同型修复，headers 为 `{ "x-api-key", "anthropic-version" }`，x-api-key 命中 redactSecrets 的 `/authorization|api-key/i` 头名正则。
- **[简单] ollama 流内 error 行被吞**：Ollama 生成中途出错时在 200 流内输出 `{"error":"... model requires more system memory ..."}`，被当作无关行跳过，最终报笼统的"流式响应未返回任何文本"（bad_response 硬失败直接短路降级链）。修复：OllamaStreamLine 增加 `error?: string | { message?: string }`（兼容两种形态），非空时抛 ApiError("server", redactSecrets(...))。
- **[简单] gemini 流内 error 帧被吞**：Gemini alt=sse 流中途失败时以 `data: {"error":{...}}` 帧结束流，在 `!Array.isArray(parts)` 处无声返回。修复：GeminiGenerateContentResponse 增加 `error?: { message?: string }`，检查 candidates 前先检查 evt.error?.message 存在则抛 ApiError，message 经 authHeaders 脱敏（覆盖 x-goog-api-key）。
- **前置配套**：http.ts 的 redactSecrets 原为模块私有，为满足修复 3-6 的调用加了 export 并补 JSDoc，函数体未动。

**background 修复批 M：splitBatch 编号剥离 + cacheVariant 掺 sourceLang（已完成，41/41 全绿）**：
- **[中等] splitBatch 编号剥离误伤正常译文**：哨兵路径对每段无条件 stripIndex、逐行路径「部分命中就剥」——以「数字+分隔符」开头的正常译文被误剥（实测"10:30 出发"→"30 出发"、"2.99 美元"→"99 美元"），损坏值写入缓存永久复用。修复：新增 stripConsecutiveIndexes(parts, expected)——只在**全段都命中编号且恰为 1..N 连续递增**（模型自加序号的唯一可信形态）时统一剥离；删除「部分命中就剥」分支与哨兵路径的无条件 stripIndex。旧语义的两个测试更新为新语义（部分命中原样保留、编号非连续递增原样保留），新增「以时间/小数开头的译文不再被误剥」正向防护用例。
- **[简单] cacheVariant 未掺 sourceLang**：sourceLang 是翻译输出的实际输入（免费通道 sl/from 参数、LLM 注入 system 上下文），但变体仅掺 format|model|customSystemPrompt——用户切换「强制源语言」后重翻同一页面命中旧源语言缓存，产生「改了设置却没生效」。修复：cacheVariant 掺入 settings.translate.forceSourceLang（掺的是用户显式设置；页级自动检测结果不属设置级变体）。batchCacheHit.test.ts 的 cacheVariantOf 辅助函数同步新公式。
- 本批修复代理中途丢失，测试调整（2 个旧语义用例更新 + 变体公式同步）由主线亲自收口验证。

**content 流水线审查（第 10 个审查域，重派后完成）**：审查 6 文件（engine/observer/scheduler/navigation/translate/index），简单 4 / 中等 5 / 难题 0，全部验证为真。确认历史项现状：suppressPageState 吞 done 的主路径已修复（off 恒放行），但由此引入入口分叉问题（发现 1）。scheduler/translate/index 三个文件未发现问题（消息监听器重复注册方面 manifest 单条目注入 + watchMasterSwitchReopen 消费式自注销已保证）。

**修复批 O：observer/navigation 3 项（已完成，26/26 抽样全绿）**：
- **[中等] body 整体替换时残留 addedRoots 把旧页 detached 节点送进调度**：SPA 换页前 300ms 防抖窗口内旧页有外部突变 → addedRoots 积累旧节点 → run() 检测 lastBody 变化即 return，roots 不清空 → 下次 run 把 detached 节点照常调度（懒翻译进 lazyUnits 永不触发泄漏；forceFull 对死内容真实发请求）。修复：body 替换分支 return 前清空 addedRoots + 整体重建 addedRootsSeen（WeakSet 无 clear）；单元过滤链首位补 `u.container.isConnected` 双保险。
- **[简单] bfcache pageshow 重译缺 isPageDisabled 检查**：用户 Alt+T 还原（setPageDisabled 持久化）→ 关标签页重访问（不译，符合预期）→ 跳走后返回（bfcache 恢复）→ 因 restoredByUser 为 false 且无 isPageDisabled 检查 → 把用户明确禁用的页面译回。修复：条件补 `!opts.isPageDisabled()`（与 SPA 延迟重译路径口径一致）。
- **[简单] MAX_PENDING_ROOTS 溢出丢弃 root 时 WeakSet 残留**：shift() 丢弃最老 root 但 seen 条目只在 run 消费时删除——被丢弃 root 永占 seen 位，重新插入 DOM 后不再入队。修复：shift 时同步 `addedRootsSeen.delete(dropped)`。
- 裁决留观两条：replaceState 仅同步 URL 时整页重译（降级处理对真实 SPA 导航有风险，等线上观察）；lazyUnits 对站点移除容器的内部滞留（isConnected 检查已拦截调度侧，强引用量级小）。

**难题 2b：OpenAI 兼容通道 SSE 流式 e2e（已完成，e2e 5/5 真实运行全绿）**：现有 e2e 只覆盖 googlefree 免费通道，OpenAI 兼容通道/流式零 e2e。调研中确认两个关键事实：①整页翻译不走 SSE（callApi 不传 stream.onDelta，withStreamFallback 直接走非流式），**划词气泡是流式唯一用户路径**（bubble → translateTextStream → Port "it-stream" → requestStream）；②playwright 1.63 的 route.fulfill body 只接受 string|Buffer 无法分段推送——改用本地 node http 服务真实分段 res.write（真实 TCP 分包比 route mock 更真实）。新用例「OpenAI 兼容通道（SSE 流式）」：设置写 openai 格式（baseUrl 指向本地 SSE 服务），3 个 delta 的事件 JSON 载荷从中间切成两次 write（间隔 25ms，data: 行跨 chunk 边界，postSSE 缓冲重组逻辑在真实浏览器网络栈中被真实覆盖），鼠标拖选 #para 触发划词气泡，断言气泡文本严格等于三段 delta 拼接、Authorization/model/stream 请求形状正确。至此 e2e 覆盖两条通道（googlefree 免费整页 + OpenAI 兼容划词流式）。

**修复批 N：engine 状态机 4 项（已完成，70/70 交叉验证全绿）**：
- **[中等] 悬停单译后 Alt+T 与工具条判定口径分叉**：suppressPageState 吞 done 使 state 停留 "off" 而 hasTranslated() 为 true——工具条按 `state === "off"` 判定（翻译整页），Alt+T 按 `hasTranslated()` 判定（restore + setPageDisabled(true) 禁用自动翻译），用户预期"翻译整页"实际只还原那一段且禁用了该页。修复：index.ts 的 toggle-translate 分支改用与工具条一致的 `engine.state === "off"` 判定，分支语义按工具条重排。
- **[中等] translateUnits 主循环无异常防护**：while 循环内 await renderBatch 与 renderer.fail 无 try/catch，DOM 操作抛错时循环中断 → void 调用点成 unhandled rejection，pendingCount 不冲减、afterGroup 不执行 → state 永久卡 "translating"、工具条按钮永久禁用。修复：主循环套 try/catch/finally——catch 记录后吞掉（fire-and-forget 调用点无人接 rejection），finally 统一收尾（原正常路径末尾的冲减+afterGroup 移入 finally，无双冲减；代次已变时跳过避免把还原后页面推回终态）。
- **[简单] afterGroup 并发组交错状态失真**：并发组 B 先完成即推 done/partial 而组 A 仍在途；stats.error 全局累计使早期组 1 错后恒置 partial。修复：新增 activeGroups 计数器（组进入请求阶段 +1、finally 收尾 -1、代次作废归零防泄漏），afterGroup 在 `activeGroups > 0` 时跳过终态推送保持 translating，最后一组完成才推终态。**实现偏离说明**：任务书建议的 `pendingCount === 0` 收敛条件会引入回归——scheduleUnits 把懒观察中未入视口、未发请求的单元也计入 pendingCount，不滚动时永远 > 0 会卡 translating（lazyViewport 测试钉住的已修回归），故改用在途组数判定，语义等价。
- **[简单] 重复赋值行**：translateAllQueuedByUser = false 连写两遍，删一行。

**测试质量修复批 J：input/bubble/offscreen 三文件补测（已完成，18/18 全绿）**：三个零覆盖高风险文件补齐测试——input.test.ts 7 例（focusin 出按钮含 data-it-ui 标记、敏感页抑制、点击翻译回填+合成 input 事件+还原标记、点击还原、总开关 await 后关闭不回填、destroy 中止在途、contenteditable 分支）；bubble.test.ts 6 例（划词直出气泡走真实 translateTextStream（Port 桩）、✕/Escape/mousedown 三路关闭含在途 Port disconnect、小按钮模式、选区塌缩重划走 else 分支先 close 的回归）；offscreen.test.ts 5 例（it-tts-play 正常链路含 return true 异步回包协议、并发 play 打断回 finished:false、it-tts-stop、onerror 回 ok:false、settle 幂等）。jsdom 环境限制的绕过方式已记录在测试文件注释（Range 无布局方法给假矩形、isContentEditable 未实现 defineProperty、URL 不能整体替换只补两个静态方法、Audio 桩需补 removeAttribute）。

### 第九轮总结（2026-09-26 收口）

**审查规模**：10 个审查域（content 流水线 / content 渲染层 / content 交互组件 / background 翻译服务 / provider 适配层 / TTS 链路 / shared 公共库 / options 与 popup / 构建与 CI / 测试质量）全部返回，约 70 项发现逐条独立验证，全部属实或按待验证裁决（修 5 条、留观 4 条）。

**修复规模**：15 个修复批（A-O）+ 2 个难题由主线亲自完成，共修复约 60 项。四个修复代理中途丢失由主线收口（批 M 测试调整）。e2e 通道广度从 1 条扩到 2 条（googlefree 整页 + OpenAI 兼容划词 SSE 流式）。

**测试净增**：基线 49 文件 516 用例 → **55 文件 580 用例全绿**（净增 6 文件 64 例，全部为真实断言——含变异自检 4/4 抓住回归、e2e 5 例真实运行非 skip）。测试基建新增：tests/helpers/chromeMock.ts 共享助手（28 文件接入 install/uninstall 对）、CI_ASSERT_E2E 断言模式防静默 skip。

**最终验证**：`npm run typecheck`（双 project）0 错误、`npm run lint`（--max-warnings=0 新口径）0 问题、`npm test` 55 文件 580 用例全绿（e2e 真实运行）、`npm run build` 产物特征保持（vite 7.3.6、content 自包含 IIFE、零 sourcemap、manifest 完整）。

**工程升级**：vite 5.4.21 → 7.3.6（嵌套 vite 8 副本消失，单线维护）；eslint 规则升级（no-explicit-any/no-unused-vars 升 error + --max-warnings=0）；zip.mjs 版本交叉校验。

**本轮留观项**（裁决记录，均已在对应批次注明）：dedupKey 32 位哈希无原文回验（秒级窗口概率可忽略）、replaceState 仅同步 URL 时整页重译（降级对真实 SPA 导航有风险）、lazyUnits 站点移除容器内部滞留（isConnected 已拦截调度侧）、anthropic/gemini 非流式只取首块与流式全量拼接不对称（多块触发概率未证实）。

