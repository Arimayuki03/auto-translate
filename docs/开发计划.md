# 开发计划

> 分阶段开发清单，每完成一项勾选 `[x]`。

## 阶段 1：工程骨架

- [x] 初始化 Vite + TypeScript + @crxjs/vite-plugin 工程
- [x] 编写 `manifest.json`（MV3，声明权限：storage、activeTab、scripting）
- [x] 添加占位图标（16/32/48/128）
- [x] 扩展可在 Chrome 开发者模式加载
- [x] 配置 Git 提交规范与 `.gitignore`（已完成）

## 阶段 2：API 层

- [x] 设计适配器接口：统一请求 / 响应 / 错误映射
- [x] 实现 OpenAI 兼容适配器（`src/background/providers/openai.ts`）
- [x] 实现 Anthropic Claude 适配器（`providers/anthropic.ts`）
- [x] 实现 Google Gemini 适配器（`providers/gemini.ts`）
- [x] 实现 Ollama 原生适配器（`providers/ollama.ts`）
- [x] 实现设置页（`src/options/`）：API 配置表单 + 测试连接按钮
- [x] 实现并发控制与批量合并（`src/background/translate.ts`）
- [x] 实现缓存（`src/background/cache.ts`，键：语言+原文哈希）
- [x] 实现错误分级与重试
- [ ] 流式输出支持（stream）
- [x] 多 API 主备配置与自动切换
- [x] API Key 本地加密存储

## 阶段 3：全文翻译（MVP）

- [x] 文本节点提取与过滤（`src/content/extractor.ts`）
- [x] 句子分块与映射表
- [x] 译文渲染与双语对照（`src/content/renderer.ts`）
- [x] 三种显示模式（双语 / 仅译文 / 原文）与一键还原
- [x] 翻译中占位动画、逐块替换
- [x] 源语言自动检测
- [x] 敏感页面保护（银行 / 登录 / 支付页面不翻译）
- [x] 最小浮动控制条（模式切换 / 还原 / 失败重试，阶段 4 替换为完整工具条）
- [x] 站点黑白名单守卫（content 侧，设置页 UI 待阶段 6 补）

## 阶段 4：交互完善

- [x] 顶部悬浮工具条（`src/content/toolbar.ts`）
- [x] 划词翻译气泡（`src/content/bubble.ts`）
- [x] 输入框翻译（`src/content/input.ts`）
- [x] 段落级"翻译"按钮（`src/content/hover.ts`）
- [x] MutationObserver 支持 SPA 动态内容（`src/content/observer.ts`）
- [x] 快捷键（翻译开关 Alt+T / 模式切换 Alt+M）
- [x] 术语表与专有名词保护（`src/content/translate.ts`，token 占位）
- [x] 无感翻译：空闲调度 + 视口优先 + 译文淡入，不弹提示不闪烁

## 阶段 5：性能与容错

- [ ] 站点黑白名单（白名单限定站点，节省额度）
- [ ] IntersectionObserver 视口懒翻译（可选开关）
- [ ] 译文回填校验（防错位）
- [ ] 样式隔离（前缀类名 + Shadow DOM）
- [ ] 错误提示 UI 与重试入口
- [ ] 缓存持久化（磁盘级，跨会话复用）

## 阶段 6：打磨与发布

- [ ] 设置页完善（缓存管理、数据导入导出）
- [ ] 深色主题适配
- [ ] 打包脚本（生成 zip）
- [ ] ESLint + Prettier 规范与单元测试（分块、适配器）
- [ ] GitHub Actions 自动构建 zip
- [ ] 译文导出 / 整页复制
- [ ] 编写使用说明（已完成初稿）
- [ ] 发布到 GitHub Releases（可选）

## 里程碑

| 里程碑 | 对应阶段 | 验收标志 |
| --- | --- | --- |
| M1 可配置可调用 | 1–2 | 设置页填 API 后能翻译任意文本 |
| M2 核心可用 | 3 | 打开英文网页可双语对照阅读 |
| M3 体验完善 | 4–5 | 划词/输入框/工具条可用，稳定不崩 |
| M4 可发布 | 6 | 打包 zip，安装即用 |
