# 后续任务与未完成事项

更新时间：2026-09-14

## 〇〇〇〇、2026-09-14 站点规则库 + 视口优先调度（路线图第 3 项，见 docs/未来方向.md）

- [x] **站点规则库**（`src/shared/siteRules.ts`）：
  - 12 条内置规则：全局数学公式区（KaTeX/MathJax/MathML）+ Wikipedia / GitHub / Discord / Reddit /
    YouTube / Hacker News / StackOverflow / ChatGPT / Medium / Notion / Substack 的「排除区」
    （导航/按钮/字幕/投票栏等无翻译价值区域），选取原则宁缺毋滥，不合意可逐条禁用。
  - URL 匹配：host 精确/子域后缀 + `*.` 多级子域 + 域内/TLD 通配（`www.amazon.*`）+ 路径前缀
    （`example.com/docs` 命中 `/docs` 与 `/docs/*`，不命中 `/docsx`）+ excludeMatches 反排除；
    非法模式（端口/非 http(s) 协议）直接丢弃不命中。
  - 合并解析 `resolveSiteRules`：内置（未被禁用）+ 用户规则并集；坏选择器逐条校验剔除、
    标签三形态（原样/大写/小写）收录；无 id 用户规则合成 `user-N` 诊断 id。页面装配时解析一次，
    热路径只消费结果。
  - 用户自定义（设置页「站点规则库」JSON 文本域，每行一条或整段数组）：excludeSelectors /
    excludeTags / forceBlockTags / excludeMatches / enabled；`sanitizeSiteRules` 逐字段校验
    （缺 matches 或无动作字段的整条丢弃，封顶 100 条），导入设置同口径。
  - 内置规则勾选列表（取消勾选落 `sites.disabledRuleIds`），保存失败（JSON 坏行带行号）有明确提示。
- [x] **提取管线接入**：`ExtractOptions` 扩展 excludeTags / forceBlockTags / excludeSelector；
  - excludeTags/excludeSelector：文本提取跳过（祖先命中经 excludedCache 传导整棵子树），
    Shadow DOM 宿主侧同判（isShadowScopeExcluded），属性翻译同过滤（placeholder 不译排除区）；
  - forceBlockTags：自定义元素强制按块级容器锚定（resolveContainer 与 BLOCK_TAGS 同权）。
- [x] **视口优先调度**（`engine.translateUnits`）：单元按 `viewportOrderKey`（ui.ts 新增：
  视口内 = r.top，视口外 = innerHeight + 边缘距离，不可见 = 最大值）稳定排序后分批；
  批次动态出队——每次出队前按批首容器重算距视口距离选最近批，滚动后未发批次跟随用户位置
  （滚动跟读首屏更快）；先完成的批先渲染（Promise.race，消除固定顺序的队头阻塞）；
  并发度仍由 FETCH_WINDOW=4 钳制，后台限流不变；还原/换页代次校验逐批保留。
- [x] 新增测试：`tests/siteRules.test.ts`（18 用例：模式匹配/合并/校验）+
  `tests/siteRulesExtraction.test.ts`（8 用例：提取/shadow/属性接入）+
  `tests/viewportPriority.test.ts`（3 用例：首请求视口优先/滚动重选/退化 DOM 顺序）；
  加固 `tests/dedupeScheduling.test.ts` 的固定 flush 为 waitFor 轮询（消除高负载偶发）；
  全部 **31 个测试文件、285 个测试用例**通过（连跑 8 轮全绿）；typecheck / lint / build 通过。
- [ ] **实机验证**（用户执行）：GitHub 上文件列表/页头不再出译文；YouTube 字幕条不受翻译影响；
  设置页添加一条自定义规则（如某站点 `.ad-banner` 不译）后刷新生效；长页翻译时先看到当前屏幕的
  译文（整页直译模式下），滚动后新批次跟随阅读位置。

## 〇〇〇、2026-09-14 LLM 上下文摘要（P2 路线图项，见 docs/未来方向.md）

- [x] **页面摘要生成**（`TranslateService.generatePageSummary`）：整页翻译期间 content 异步发
  `page-summary` 消息（携带截断正文 + sessionId），background 用 LLM 生成 2-3 句文章摘要
  （`SUMMARY_SYSTEM_PROMPT`，输出截断 600 字符）；与翻译共用启动限速 / 并发限制 / 429 冷却，
  best-effort 至多一次重试，失败静默回 ""（content 保持原文截断上下文，不阻塞不报错）。
  还原/换页按 sessionId 一并中止，SW 保活覆盖。
- [x] **摘要缓存**（`SummaryCache`，`src/background/cache.ts`）：内存 + `it-summary:` 磁盘前缀，
  键 = 变体(通道格式+模型)+标题+正文长度+正文 哈希，条目 src 存「标题|正文」哈希做碰撞校验
  （单条 ~100 字节，不撑爆存储配额）；TTL / 条目上限随缓存设置刷新，纳入每日清理、
  「清空缓存 / 立即清理 / 缓存统计」。同页并发摘要请求跨标签页去重（summaryInflight）。
- [x] **上下文注入**：`TranslationContext.summary` → 批量请求 system 提示词新增「文章摘要：…」字段
  （标题/描述/正文截断保留原样）；summary 缺省时提示词与历史版本完全一致。
- [x] **触发条件**（`getPageContext` 升级）：摘要开启时同一次 TreeWalker 顺带统计正文字数
  （计到阈值即停，无额外遍历成本）；正文 ≥ `summaryMinChars`（默认 6000，0 = 不限）且
  正文非空才请求摘要。首批请求先带原文截断上下文发出，摘要返回后的批次自动携带
  （`enrichPageSummary` 代次 + 上下文引用双校验，防还原/换页串页）。
- [x] **设置项**：`translate.summaryEnabled`（默认关）/ `translate.summaryMinChars`（默认 6000）；
  设置页「页面上下文」区块新增开关与阈值输入，导入设置逐字段校验；免费通道
  （googlefree/microsoft）不支持摘要，自动跳过回退原文截断。
- [x] 新增测试：`tests/pageSummary.test.ts`（缓存读写/TTL/碰撞/清理、生成短路/缓存命中/失败/
  并发去重/中止、上下文注入）+ `tests/pageSummaryEngine.test.ts`（jsdom：长文页首批无摘要、
  摘要后批次携带，短页/关闭不触发）；全部 **28 个测试文件、259 个测试用例**通过；
  typecheck / lint / build 通过。
- [ ] **实机验证**（用户执行）：开启摘要后翻译一篇长文（≥6000 字），确认请求多一次、
  后续批次术语一致性提升；关闭开关 / 短页不产生额外请求；免费通道下无摘要请求。

## 〇〇一、2026-09-14 划词朗读 TTS（P2 路线图项，见 docs/未来方向.md）

- [x] **气泡「朗读」按钮**：划词译文气泡操作行新增「朗读」（在「复制」左侧），
  朗读流式结束后的完整译文；流式未结束/翻译失败态点击无效。状态：朗读 → 生成中… → 停止 → 朗读，
  失败短暂显示「朗读失败」（title 带具体错误）后自动复位；关闭气泡（✕/Esc/滚动/新划词/点击外部）即停止播放。
- [x] **Edge TTS 免费合成**（`src/background/edgeTts.ts`，无需 API Key）：
  MSTranslatorAndroidApp HMAC-SHA256 签名换取 dev.microsofttranslator.com 端点令牌（JWT ~10min，缓存复用，
  提前 3 分钟刷新）→ POST SSML 到 `{region}.tts.speech.microsoft.com` 合成 MP3（audio-24khz-48kbitrate-mono-mp3）。
  401/403 清令牌重取重试一次；文本超 3000 字符截断；控制字符/XML 转义清理。
- [x] **声音解析**：设置显式指定 > 目标语言精确匹配（zh-CN→晓晓、en→Ava、ja→七海、ko→善熙 等 16 语言）
  > 语言前缀匹配（en-GB→en）> 默认 en-US-AvaNeural。语速 -50%~+100%（SSML prosody rate）。
- [x] **熔断**：10 分钟窗口内 5 次失败 → 熔断 15 分钟（期间请求直接报「朗读服务暂时不可用」，零网络请求），
  成功即清零。
- [x] **offscreen 播放**（`src/background/ttsPlayback.ts` + `public/offscreen.html/js`）：MV3 SW 无 DOM，
  按需创建 offscreen 文档（AUDIO_PLAYBACK）转发播放；全局同一时刻至多一条，「最新请求优先」打断旧播放；
  播放期间 keepAlive 保活 SW；offscreen 被浏览器回收时重建重试一次。
  offscreen 页为 public/ 下的普通 JS（扩展页 CSP 禁止内联脚本，也不经打包，零依赖）。
- [x] **设置项**：`tts: { enabled, voice, rate }`——设置页新增「划词朗读（TTS）」卡片（开关/声音/语速），
  导入设置逐字段校验；旧设置无 tts 段时合并默认值（enabled=true），无需版本迁移。
- [x] 新增测试：`tests/ttsSynthesize.test.ts`（声音解析/SSML 构造/合成请求形状/令牌缓存/401 重取/
  空音频/熔断/导入校验）+ `tests/ttsController.test.ts`（内容侧状态机 7 用例）；
  全部 **26 个测试文件、240 个测试用例**通过；typecheck / lint / build 通过。
- [x] 已验证：构建产物含 offscreen.html/js 与 offscreen 权限；设置页冒烟测试（HTTP 服务 + chrome mock）
  确认旧设置升级默认合并、表单回填、修改保存落盘正确。
- [ ] **实机验证**（用户执行）：真实页面划词 → 朗读有声（Edge/Chrome），播放中停止、关气泡即停、
  连续划词不打架；Edge TTS 偶发限流时提示是否友好。

## 〇〇、2026-09-14 第二轮代码审查修复（详见 docs/代码审查报告.md 第二轮章节）

- [x] G-1（P1）：MV3 SW 空闲回收可打断长翻译——新增 `keepAlive.ts`，整页翻译/划词流式在途期间每 20s
  no-op 扩展调用保活，结束自动停止。
- [x] G-2（P1）：同页锚点跳转（仅 hash 变化）不再误判 SPA 换页清空整页译文重译。
- [x] G-3（P1）：划词气泡内滚动不再关闭气泡（捕获阶段 scroll 判断 target）。
- [x] G-4（P1）：SPA 换页打断在途翻译后 state 兜底回 done，工具条按钮不再永久禁用（新增回归测试）。
- [x] G-5（P1）：域名匹配收紧为单向子域——黑/白名单条目不再放大到整个主域。
- [x] G-6（P2）：缓存键掺入 format/model/customSystemPrompt 变体——换配置后旧译文不再命中（新增回归测试）。
- [x] G-7（P2）：限流按「格式+端点」分通道——免费通道 429 冷却不再冻结主/备 API。
- [x] G-8（P2）：2xx 非 JSON 响应包为 bad_response 走统一错误分级与脱敏诊断。
- [x] G-9（P2）：删除 `data-it-orig-html` innerHTML 兼容路径（XSS sink）。
- [x] G-10（P2）：渲染层 byContainer/controlContainers 失连条目即遍历即清（内存泄漏）。
- [x] G-11（P2）：观察器点击扫描跳过自身 UI 并限流约 1s/次；G-12：悬停提取结果按元素缓存 1s。
- [x] G-13（P2）：设置导入逐字段类型校验——类型错乱的导入文件不再导致全页注入失败（新增回归测试）。
- [x] G-14（P2）：缓存磁盘写入失败不再误推进 diskKeys；抽取 googlefree/microsoft 公共解析为
  `providers/segments.ts`；移除死代码 `batchSeparatorLine`。
- [x] 测试稳健性：sessionCancel 固定 flush 改 waitFor 轮询（修高负载偶发失败）。
- [x] 全部测试通过：24 个测试文件、217 个测试用例；typecheck / lint / build 通过。

## 〇、2026-09-14 代码审查修复（详见 docs/代码审查报告.md）

- [x] F-1（P1）：googlefree 固定哨兵协议——默认逐行配置下不再把逐行指令前缀当待译段发给 Google，
  批量解析恢复一次成功，消除整页翻译请求量翻倍（`callApi` 按通道决定协议 + 新增回归测试）。
- [x] F-2（P1）：还原/换页时在途批次不再污染 `doneTexts`、清理残留 `data-it-processing`，
  「还原后再翻译」不再永久缺段（`fetchBatch` generation 校验 + `clearProcessingMarks` + 回归测试）。
- [x] F-3（P1）：会话取消接线到 fetch——`ChatOptions.signal` 贯通 `postJson` / `gTranslate`，
  还原/换页能真正打断在途 HTTP 请求（中止抛普通 Error，不触发重试/备用/失败上报 + 回归测试）。
- [x] F-4（P1）：译文缓存写入 `{src, val}` 并在读取时校验原文——32 位哈希碰撞只导致一次未命中，
  不再可能返回错误译文；空译文不入缓存；历史纯字符串条目兼容（新增回归测试）。
- [x] F-5（P2）：observer `addedRoots` 设上限 FIFO + `addedRootsSeen` 改 WeakSet，
  off/hidden 期间动态页面不再无界积累节点引用（内存泄漏）。
- [x] F-6（P2）：观察器点击兜底全量扫描改用时间片版 `extractUnitsChunked`（每 12ms 让出主线程）。
- [x] F-7（P2）：Anthropic 输出预算随输入放大（≤8192），模型 400 拒绝时降回 4096 重试一次。
- [x] E-003：`vite.config.ts` 关闭 modulePreload——content 脚本隔离世界的跨世界预加载警告消失。
- [x] 请求间隔可配置：设置页新增「请求间隔（毫秒）」（`api.minRequestIntervalMs`，默认 500，
  钳制 50–10000）——中转站限额严格时调大（如 1000～2000）。
- [x] 降级放大上限：批量「解析失败」（非 API 报错）时先拆 ≤8 段小批量重试，仍失败的组才逐段——
  最坏情况请求数从 N 压到约 N/8，且逐段请求不再重复携带整页上下文（省 token/TPM）。
- [x] 全部测试通过：16 个测试文件、121 个测试用例。

### 2026-09-14 后续增量

- [x] 译文显示布局优化（commit 0cd64c9）：骨架屏占位（替代不可见空白）、灰字颜色从页面文字色
  派生（修复深色系统 + 浅色页面看不清）、多 chunk 流式拼接为自然段、占位高度按目标语言估算 +
  真实行高（填充不再跳页）、行内控件占位不撑破导航。已用 demo 页在浏览器中截图验证。
- [x] 对照 read-frog 的对比与路线图：见 `docs/未来方向.md`（P0：样式主题/属性翻译/Microsoft
  免费通道/缓存 TTL/超时缩放与请求去重；P1：划词流式/iframe/Shadow DOM/站点规则等）。
- [ ] **实机验证**（用户执行，已确认延后：先完成 P0 一揽子，之后统一验证）：DeepSeek 翻译、
  googlefree 提速、还原后重翻不缺段、还原时在途请求 cancelled、请求间隔设置生效。
- [x] P3：移除 [IT-PERF] 埋点；`.gitignore` 增加 `read-frog-main/` 与 `.qa/`；
  options 页并发兜底值 3→2 并钳制 ≥1；v2→v3 迁移注释修正；googlefree 目标语言显式传递（`ChatOptions.targetLang`）。
- [x] 全部测试通过：16 个测试文件、118 个测试用例；`npm run typecheck` / `npm run lint` / `npm run build` 通过。

### 2026-09-14 P0 一揽子（对照 read-frog 补齐核心体验，见 docs/未来方向.md）

- [x] **译文样式主题**：新增 4 种主题——灰字（默认）/ 描边（空心字）/ 虚线下划线 / 模糊（悬停显形，
  触屏退化为灰字）；body 类切换（`src/content/style.ts` + content.css 主题作用域），SPA 换页后自动重挂；
  另支持「自定义译文 CSS」（`translate.customCss`，`<style data-it-ui>` 注入，剥 `</style>` 逃逸、限长 8000）；
  设置页新增「译文样式」下拉与自定义 CSS 文本框（保存后刷新页面生效）。
- [x] **HTML 属性翻译**：placeholder / title / alt / aria-label 并入统一属性管线（收集 → 文本去重批译 →
  `data-it-attr-orig` 保存原文 → 一键还原恢复）；跳过我们的 UI、SVG、aria-hidden、translate=no、
  可编辑区与目标语言文本；候选上限 300 元素、单值上限 500 字符；开关 `translate.translateAttributes`（默认开）。
- [x] **Microsoft 免 key 通道**：`microsoft` provider（`edge.microsoft.com/translate/translatetext`，
  裸 JSON 字符串数组，端点原生批量、50 段/请求；`< > &` 转义往返防标签对齐器吃字符）；
  **免费通道自动互切**——主通道为 googlefree/microsoft 之一且未配置备用 API 时，可重试失败自动切到另一个；
  设置页/popup 格式下拉新增选项；测试连接按免费通道语义处理。
- [x] **缓存 TTL + 定期清理**：条目新增写入时间戳 `ts`，`cache.ttlDays`（默认 7 天，0 = 永不过期）过期即未命中；
  chrome.alarms（`it-cache-cleanup`，manifest 新增 `alarms` 权限）每日清理过期/历史格式/损坏条目并按
  `maxEntries` 保留最新 N 条——磁盘层不再只进不出；设置页新增条目数展示、「立即清理」按钮。
  兼容性说明：历史纯字符串缓存条目不再命中（重译一次后以新结构落盘）。
- [x] **批量超时缩放**：请求超时 = 配置值 + 15ms/字符，封顶 max(配置值, 120s)——大批量「还在正常生成」
  不再被误判超时重试放大请求量。
- [x] **跨标签页请求去重**：文本 + 目标语言相同的并发请求共享同一次执行（多标签页同翻一个站点不重复耗额度）；
  共享请求被其他会话中止时，仍在翻译的等待者自行重发，不把中止扩散给无辜方。
- [x] 全部测试通过：**20 个测试文件、151 个测试用例**（新增 microsoftProvider / requestDedup /
  styleTheme / attributeTranslation 4 个测试文件 + 缓存 TTL 用例）；
  `npm run typecheck` / `npm run lint` / `npm run build` 通过。

### 2026-09-14 P1 并行一揽子(3 个子代理并行完成,见 docs/未来方向.md)

- [x] **划词翻译流式输出**:气泡翻译改走 Port 长连接(`it-stream`),background 监听 onConnect 转发 provider SSE 增量
  (openai/anthropic/gemini/ollama),rAF 节流逐字渲染;气泡任何关闭动作(✕/Esc/滚动/点击外部/新划词)= port 断开 =
  请求中止(复用 signal 管线,中止不重试/不切备用/不报错);未产增量时按既有降级链回退(非流式 → 重试 → 免费互切 → 备用);
  全页/输入框翻译路径不变;免费通道与缓存命中由 done 一次性回传全文。
- [x] **自定义翻译 prompt**:`api.customSystemPrompt`(设置页「自定义翻译指令」textarea,限长 2000)作为附加指令拼在
  现有 system 之前,批量协议指令始终保留,不破坏批量格式;免费通道不适用。
- [x] **{{NO_TRANSLATION_NEEDED}} 免译哨兵**:LLM 批量指令允许模型对「本身是目标语言/代码/公式/编号」段落原样输出
  哨兵,解析侧忽略首尾空白的严格相等映射回原文(不重试不报错);批量原文含哨兵字样时本批不注入(与 ===IT_SEP=== 同模式);
  免费通道仅解析兜底;googlefree 固定哨兵协议(F-1)不受影响。
- [x] **Shadow DOM 提取**:遍历扩展进 open shadow root(light/shadow 域子树互不相交,天然不重复提取),shadow 内文本
  照常成单元;renderer 首次插入时向 shadowRoot 注入 adoptedStyleSheets(样式子集常量 + 按当前显示模式/主题直出,
  无 Constructable Stylesheet 时回退 shadow 内 `<style data-it-ui>`),setMode 时同步重建。
- [x] **巨型段落拆分**:提取阶段超过 1200 字符的文本块按句子边界均衡拆为多个 chunk(中文句读必切、英文句读后跟空白
  才切,`3.14`/`e.g` 不误切;无边界按硬上限强切),chunk 进同一容器,批量/渲染/去重口径不变。
- [x] **iframe 支持**:manifest content_scripts 开 `all_frames: true`;子 frame 装配引擎/观察器/守卫(按本 frame URL
  动态判断凭据页与黑白名单),裁掉工具条/划词气泡/输入框/SPA 接管;Alt+T 经 background `tabs.sendMessage`
  (无 frameId,已核实为广播)各 frame 独立 toggle;autoTranslate 各 frame 按自身 visibilityState 自译。
- [x] **悬停翻译**:顶层 frame、页面未整体翻译时,悬停块级文本容器出现「译」角标,点击只译该段(engine 新增最小 API
  `translateElement`,复用提取/调度/去重/失败标记/生成号校验;单元素翻译期间钳制页面状态,防止观察器把整页补译);
  排除我们的 UI/表单控件/隐藏元素/aria-hidden/<20 字符/已译元素;整页翻译后角标自动关停,还原后恢复。
- [x] 全部测试通过:**24 个测试文件、214 个测试用例**(新增 streaming / shadowDom / giantParagraph / hoverIframe
  4 个测试文件);`npm run typecheck` / `npm run lint` / `npm run build` 通过。
- 备注(并入下一轮实机验证):带 shadow 的站点(GitHub 评论组件)、iframe 站点、悬停角标、气泡流式;
  工具条按钮暂不作用于子 frame(快捷键/自动翻译已覆盖;跨 frame 同步需 background 新消息,见未来方向)。

## 一、当前已完成

- [x] 哨兵分隔符批量翻译：批量请求使用 `===IT_SEP===`，解析优先按哨兵切分，并保留按行解析兜底。
- [x] Google 免费翻译通道：新增 `googlefree` provider，支持无 API Key 翻译。
- [x] 上下文感知翻译：全页翻译携带页面标题、描述和正文摘要；划词/输入框翻译不携带上下文。
- [x] Google 免费通道响应格式修复：按 `translate_a/single` 的数组响应解析。
- [x] 主 API 备用通道：保留原有“仅可重试错误切换备用 API”的逻辑，避免改变第三方站点原有行为。
- [x] 第三方 API 配置保留：切换 Google 免费通道时不清空原有 BaseURL、API Key 和模型输入框内容。
- [x] **批量协议回归修复（本阶段）**：默认恢复旧版逐行协议，哨兵协议仅在 `batchMode: "separator"` 时启用。
  - `callApi` 按 `settings.api.batchMode` 构造 system/user messages；`lines` 与历史版本逐字一致。
  - `googlefree` 内部固定使用安全哨兵协议（其 provider 会忽略 batchMode），避免把逐行指令前缀当待译段。
  - 批量解析失败时：`separator` 失败先回退一次 `lines`（仍是一次请求），再失败才逐段降级，避免请求风暴；
    `lines` 失败直接逐段降级，不向不支持哨兵的第三方模型反复试探哨兵提示词。
  - 批量选项（batchMode / batchSeparator / batchSize / freeEndpoint）现在会透传给 provider。
- [x] **设置页 Google 免费通道切换不再清空/抹掉第三方配置（本阶段）**：
  - options 页 `updateFormatHint`：切到 googlefree 只禁用输入框、不清空原值。
  - options 页 `readForm`：透传 `batchMode` / `freeEndpoint` / `freeBackupEndpoint`，保存不丢。
  - popup 页 `readApi`：不再把 baseUrl/apiKey/model 强制置空，保存不会抹掉原有第三方配置。
- [x] **错误诊断兜底（本阶段）**：`buildApiUrl` 对空/非法 BaseURL 抛清晰 `ApiError(bad_request)`，
  不再出现 `new URL("")` 的 TypeError 被误报为网络错误/鉴权失败。
- [x] **PageEngine 调度去重补全（本阶段）**：
  - `translateAll` / `observer.run` 与引擎同一去重口径（data-it-src / data-it-processing / isFailed / isScheduled / isSkipped）。
  - 翻译失败的容器不再被 MutationObserver / 点击扫描反复重新调度（`renderer.isFailed` 过滤），total 不再膨胀。
  - 上下文注入接入 `contextEnabled` / `contextMaxChars` 设置，正文摘要按上限截断。
- [x] 移除死代码 `shouldUseBackup`（未使用，且其注释与“仅可重试错误切备用”的既定行为矛盾）。
- [x] **设置页补齐新字段 UI（本阶段）**：
  - 批量翻译协议下拉（逐行=默认兼容 / 哨兵），googlefree 时禁用（内部固定哨兵）。
  - 免费通道主/备用端点输入框（仅 googlefree 显示），写入 `freeEndpoint` / `freeBackupEndpoint`。
  - 上下文开关 + 上下文最大字符数（写入 `contextEnabled` / `contextMaxChars`）。
  - 「测试主 API」「测试备用 API」独立按钮（`runTestConnection` 复用，失败信息带来源前缀）。
- [x] **错误诊断端到端打通（本阶段）**：
  - `TranslateService.translate` 记录首个可诊断错误；需请求的段落全部失败时**上抛**（带 code + diagnostic），不再静默返回空串。
  - `callApi` 用 `withErrorSource` 给错误标注「主 API / 备用 API」来源，401/403 显示为「主 API 鉴权失败」，不再误报为免费通道错误。
  - background 的 translate / test-connection 响应回传 `errorCode` + 脱敏 `diagnostic`。
  - 内容侧 `translateTexts` 抛 `TranslateError`（带 code/diagnostic）；引擎 `lastError` 留存并在工具条状态行显示具体失败原因。
  - 工具条新增「复制诊断信息」按钮：复制 provider/端点/主机/HTTP 状态/错误码，**不含 API Key**。
- [x] **哨兵冲突防护 + 批量边界测试（本阶段）**：
  - 原文本身含 `===IT_SEP===` 时强制逐行协议，避免哨兵分段歧义。
  - 新增 splitBatch 边界测试：Markdown 代码块 / 行内代码 / 段内编号 / 空段漏译 / 空行 / 多行段落。
- [x] **SPA 换页清理旧上下文（本阶段）**：`resetForNavigation` 作废 `pageContext`，
  避免换页后观察器先于 `translateAll` 触发翻译时把旧页标题/摘要当作新页语境注入。
- [x] 全部测试通过：16 个测试文件、110 个测试用例（2026-09-14 审查修复后增至 118，见「〇」节）。
- [x] `npm run typecheck` 通过。
- [x] `npm run build` 成功。

## 二、当前未完成 / 需要优先处理

### P0：第三方 API 回归的实机复核

- [ ] 使用原有第三方 API 配置复测：确认 BaseURL 拼接（`buildApiUrl` 兼容尾斜杠/重叠路径）、
  请求头、模型、API Key 解密、system/user messages 与历史版本一致（已加回归测试锁定，仍建议实机确认）。
- [ ] 逐行协议恢复后，用真实第三方站点（DeepSeek / OneAPI / 中转）复测正常翻译。
- [ ] 确认第三方站点是否因为批量哨兵提示词拒绝请求（已通过“默认逐行 + 仅配置启用哨兵”规避，实机确认）。

### P0：验证设置兼容性（已加单测，待实机确认）

- [ ] 用历史版本保存的 `version: 4` 设置升级本版本后，检查所有字段原样保留（单测已覆盖）。
- [ ] 导入/导出设置后，实机确认 API Key、BaseURL、模型、provider 正确恢复（单测已覆盖）。

## 三、下一阶段任务

### P1：免费翻译通道增强

- [x] Google 免费通道增加可配置端点或备用端点的设置页入口（已加 UI 并写入 freeEndpoint / freeBackupEndpoint）。
- [ ] 增加请求频率限制和退避，避免大量页面触发 429（已有 ≤3 并发 + 80ms 起步间隔，可再评估）。
- [x] 免费通道失败时显示明确错误：网络不可达、频率限制、响应格式错误（provider 层已区分，错误诊断已端到端打通到工具条）。
- [x] Microsoft 免费通道已接入（含与 googlefree 的 429 自动互切，见「〇」P0 一揽子）；
  剩余候选：DeepLX 或用户自定义 HTTP 翻译接口。
- [ ] 免费通道不应默认覆盖用户已配置的第三方 LLM（已保证，待实机确认）。

### P1：上下文感知翻译优化

- [ ] 当前上下文只在全页批量翻译注入，确认不会影响第三方 API 的 token 限制。
- [x] 为不同站点限制正文摘要长度（`contextMaxChars` 可配置，正文按上限截断）。
- [ ] 避免将登录、支付、密码等敏感页面内容加入上下文（敏感页已整体跳过自动翻译，待确认上下文注入时机）。
- [x] 上下文变化时重新计算，SPA 换页时清理旧上下文（`resetForNavigation` 作废 `pageContext`，观察器先触发的翻译不再带旧页语境；回归测试 `tests/spaContext.test.ts`）。
- [x] 增加上下文开关和最大字符数配置的设置页 UI（`contextEnabled` / `contextMaxChars`）。

### P1：批量翻译兼容模式

- [x] 保留旧版按行批量协议作为默认兼容模式。
- [x] 对支持哨兵的模型启用哨兵模式（`batchMode: "separator"`）。
- [x] 哨兵模式解析失败时自动重试旧版批量格式，而不是直接大量降级为单段请求。
- [x] 防止原文中包含 `===IT_SEP===` 时破坏分段（含哨兵原文强制走逐行协议 + 回归测试）。
- [x] 设置页提供批量协议选择 UI（`batchMode` 下拉）。
- [x] 增加包含换行、空段、编号、Markdown 和代码片段的批量测试。

### P2：错误诊断

- [x] 在工具条显示具体错误类型和失败数量（partial 状态行展示 `lastError.message`）。
- [x] 401/403 显示“主 API 鉴权失败”，不要误报为 Google 免费通道错误（`withErrorSource` 已接线，主/备来源标注）。
- [x] 记录脱敏后的 provider、URL 主机名、HTTP 状态和错误码（`makeDiagnostic` / `redactSecrets`）。
- [x] 增加设置页“测试主 API”和“测试备用 API”的独立按钮。
- [x] 提供“复制诊断信息”功能，禁止包含 API Key（工具条按钮，仅端点/主机/状态/错误码）。

## 四、推荐执行顺序

1. [x] 隔离/回滚第三方 API 的破坏性改动（恢复逐行协议为默认）。
2. [x] 修复 `PageEngine` 重复调度和计数不断增加问题。
3. [ ] 用第三方站点实际配置复测正常翻译（实机确认）。
4. [x] 添加请求协议和鉴权回归测试（`tests/providerRequests.test.ts`）。
5. [x] 重新设计哨兵批量模式的兼容策略（默认逐行、配置哨兵、失败回退）。
6. [x] 完善 Google 免费通道和上下文感知翻译的设置页 UI（实机验证待做）。

## 五、验收标准

- [x] 原来可用的第三方 API 在不修改配置的情况下恢复正常（默认逐行协议，需实机最终确认）。
- [x] 不再出现无实际请求或错误信息不匹配的“鉴权失败”（空 BaseURL 报清晰 bad_request；401/403 标注主 API 来源）。
- [x] 同一页面同一文本不会因为 MutationObserver、SPA 或可见性事件重复请求（去重口径统一 + 回归测试）。
- [x] 翻译总数在页面稳定后不再持续增加（失败容器不重复调度）。
- [x] 批量失败最多有限次降级，不形成请求风暴（逐行失败直接逐段；哨兵失败回退一次逐行）。
- [x] `npm run typecheck` 通过。
- [x] `npm test` 全部通过（16 文件 / 118 用例）。
- [x] `npm run build` 成功。
