# 代码审查结论（第二轮）

> 日期：2026-09-18
> 范围：全仓库 `src/`（约 12,600 行，44 个 TS/CSS 文件）、`manifest.json`、`vite.config.ts`、`scripts/`
> 方法：按 background / content 引擎渲染 / content 交互 UI / 共享模块与设置页 四个子系统并行通读，主审对高危项用可执行代码独立复现
> 测试基线：`tsc --noEmit`、`eslint` 干净；**42 个测试文件 / 420 个用例全绿**（问题均为既有测试未覆盖的盲区）
> 与 `结论1.md` 的分工：结论1 审的是本轮改动本身（keepAlive 修复 + 5 个新测试，含变异测试发现的空转用例）；本文件是全仓库系统性审查。

## 结论概览

| 级别 | 数量 | 说明 |
|------|------|------|
| P0 | 4 | 已用可执行代码复现其中 3 条，另 1 条代码路径完整确认 |
| P1 | 20 | 含渲染可逆性、取消语义、请求放大、误判类 |
| P2 | 20+ | 健壮性/可维护性，见附录清单 |
| 误报 | 2 | 见文末「误报澄清」 |

**修复建议顺序**：P0-1、P0-2、P0-4 改动局部、可加回归测试，优先修；P0-3 需引入 tab/frame 维度键，改动面稍大，紧随其后。

---

## P0（必修）

### P0-1 `restore()` 跳过已脱离文档的容器 → 孤儿译文块永久残留
**位置**：`src/content/renderer.ts:580`（`if (!container.isConnected) continue;`），循环结束后 `renderer.ts:596` 执行 `byContainer.clear()`

**问题**：解包循环对失连容器直接 `continue` 跳过，随后清空索引。此时仍留在页面里的 `.it-wrap`（内含我们的 `.it-translated`）**永久脱离一切索引**——后续任何 `restore()` / `setMode()` / `translateAll()` 都找不到它，提取器又因 `data-it-unit` 跳过该子树（`extractor.ts:388`），只能刷新页面。`setMode()` 的同类 `delete`（`renderer.ts:282-291`）会更早触发同样后果。

**触发场景（实测复现）**：翻译一段 → 站点移除该容器（React 重渲染 / 虚拟列表回收，保留 wrap 层）→ 点还原。实测结果：`.it-wrap` 与 `.it-translated` 各残留 1 个，页面显示「一段没有原文的译文」；仅译文模式下该残块被 `content.css:197` 的 `display:block !important` 强制显示为正文。

**验证**：✅ 用真实 `Renderer` 走 `reserve()`→`fill()`→移除容器→`restore()`，断言失败（残留 1）。

### P0-2 `translateAll()` 排队重入覆盖用户刚做的「还原」
**位置**：`src/content/engine.ts:253-259`（`finally` 里无条件重跑 `translateAllQueued`）+ `engine.ts:199`（重跑时 `userRestored = false`）

**问题**：在途 `translateAll` 期间用户还原（`generation++`、`state="off"`、`userRestored=true`）后，在途任务返回时 `finally` 无条件把排队的 `translateAll()` 再跑一遍；新一轮立刻重置 `userRestored` 并重新调度全页翻译。`isPageDisabled()` 只是 content 侧门控，引擎内部无此判定，无法自保。

**触发场景（实测复现）**：大页面自动翻译进行中按 `Alt+T`（工具条按钮此时 disabled，快捷键仍可用）触发 `restore()` → 页面闪回原文后又被整页译回。实测结果：还原后 `.it-wrap` 重新出现 2 个，`userRestored` 被清掉（后续 `visibilitychange` 还会继续补译），并额外消耗 API 额度。

**验证**：✅ 用真实 `PageEngine` + 挂起首个请求模拟在途，断言失败（残留 2）。

### P0-3 `sessionId` 未按标签页 / frame 隔离 → 跨页误杀在途翻译
**位置**：`src/background/index.ts:39-54`（`sessionControllers` 为 SW 全局 Map）、`background/index.ts:121-129`（按数字 abort 整个 set）；产生方 `src/content/engine.ts:92`（每实例 `generationValue = 0`）、`engine.ts:545` / `engine.ts:419-420` / `engine.ts:458-459`

**问题**：`sessionId` 是各 frame 从 0 开始的引擎代次，而 background 按这个裸数字索引控制器集合，**无 tab/frame 维度**。`manifest.json` 开启 `all_frames: true`，每个 iframe 各有独立引擎，也各从 0 开始。

**触发场景**：同站点两个标签页各开一份并自动翻译（都是 generation 0）→ A 页点还原或 SPA 换页 → 发出的 `cancel-translation {sessionId:0}` abort 掉 B 页在途请求 → B 页整批显示「翻译失败」占位并计入失败数。同页 iframe 与顶层 frame 同样互杀；`page-summary` 请求（`engine.ts:316`）用同一键空间，也会被顺带中止。

**验证**：✅ 代码路径完整确认（无任何 tabId/frameId 参与键）；未做跨标签页实机复现。

### P0-4 导入任意 JSON 会静默清空全部设置（含 API Key、黑白名单）
**位置**：`src/shared/storage.ts:242-263`（`importSettings`），UI 入口 `src/options/main.ts:481-495`

**问题**：`importSettings` 只校验「是对象」与 `api.format` 白名单，然后把「认不出的字段全部丢弃」的结果与默认值合并出**完整设置**落盘（`storage.ts:259-260`）。文件里没有的字段一律回退默认值，而非「只更新出现的字段」。

**触发场景（实测复现）**：先保存 `apiKey="sk-my-real-key"`、白名单 `["keep.example.com"]`、黑名单 `["bank.com"]`；再误选同目录 `package.json` 导入 → **不报错**，UI 提示「设置已导入 ✔」，而实测读回 `key="" whitelist=[] blacklist=[]`。导入 `{}` 同样清空。

**连带问题（实测复现，建议一并修）**：`importSettings` 落盘不经 `encryptApiKey`（`storage.ts:260`），而读取无条件 `decryptApiKey`（`storage.ts:107`）。明文 Key 若恰好是合法 Base64 形态会被当密文解成乱码——实测 `abcd1234` → `"3ìE3§"`、`sk12345678` → 乱码、AWS Key 形态 `AKIAIOSFODNN7EXAMPLE` → 乱码；而 `sk-original-key`（含 `-`，触发 atob 异常回退）反而正常。**现有测试 `settingsCompat.test.ts` 恰好只覆盖后者，因此未测出。**

**验证**：✅ 用真实 `storage.ts` + mock storage 实测复现（严格 `atob` 语义）。

---

## P1（应修）

### 渲染可逆性（content）

| # | 位置 | 问题 | 验证 |
|---|------|------|------|
| 1 | `renderer.ts:437`、`:497-504`、`:961-993` | 嵌套单元的 `data-it-orig-text` 快照与还原时 `collectTextNodes` 的过滤基准（`data-it-src`）取自不同时刻 → 还原把内层原文重复追加到外层末尾，或内层原文永久丢失 | 静态确认 |
| 2 | `renderer.ts:154-161` + `engine.ts:236`、`observer.ts:159`、`hover.ts:160` | `data-it-src` 是永久过滤标记且无文本校验：站点就地改写已译容器文本后，旧译文常驻、新文本永不重译 | 静态确认 |
| 3 | `renderer.ts:610`、`:620`、`:656` | 虚拟列表复用节点时 `data-it-src` / `data-it-inside` 残留（`data-it-inside` 全仓库只写不读不清）→ 节点被永久排除翻译 | 静态确认 |
| 4 | `renderer.ts:583-588`、`:675-678` | 容器被站点搬走后还原，会把它从新位置摘回旧包裹位置，破坏站点结构 | 静态确认 |
| 5 | `engine.ts:455-477`、`renderer.ts:723-746`、`:316-329` | `restore()` 不清理 shadow 样式表、`lastError`、`unitByContainer` 失连条目；`textTranslations` 全程无上限（`doneTexts` 有 10000 上限，这里没有） | 静态确认 |
| 6 | `engine.ts:841-846` | 渲染中断分支把旧会话 `filled/failed` 累加到已清零的新会话 `stats` 并 `emitStats()` → 工具条计数虚高、影响 `hasTranslated()` 判定与快捷键分支 | 静态确认 |

### 状态机与调度（content）

| # | 位置 | 问题 | 验证 |
|---|------|------|------|
| 7 | `engine.ts:69-76`、`:877-883` + `toolbar.ts:225-243` | `suppressPageState` 吞掉 `done` 使 `state` 恒为 `off`：悬停单译后点工具条按钮触发**整页翻译**而非还原（按钮语义反转，且清掉该页 disabled 标记） | 静态确认 |
| 8 | `observer.ts:141` | 同一根因：`state` 恒 `off` 使观察器在悬停单译期间对所有动态内容停止工作 | 静态确认 |
| 9 | `observer.ts:89-112` | `isOwnNode` 把 `.it-orig` / `.it-wrap` 当自身标记 → 站点写入已译块内部的新内容（评论新增、价格刷新）被误判为自己产物，永不加入增量扫描 | 静态确认 |
| 10 | `content/index.ts:228` + `popup/main.ts:150-162` | 白名单不接受通配符：用户写 `*.example.com` 经 `parseDomainList` 原样保留 → `matchesDomain` 永不命中 → `whitelist.length > 0` 使**全站静默停止翻译**，无任何提示 | 静态确认 |

### 后台请求层（background）

| # | 位置 | 问题 | 验证 |
|---|------|------|------|
| 11 | `translate.ts:851-860`、`:844-848` | `abort` 无法打断限速 / 429 冷却的 `sleep`（上限 5 分钟）：已取消请求仍占全局并发槽与 keepAlive 计数 → 其他标签页翻译被阻塞 | 静态确认 |
| 12 | `translate.ts:323-337`、`:364-382` | 批次级 API 硬失败（401/404/5xx/429）后仍对整批**逐段重发并各自重试**：30 段 → 31 次 401；把 429 算上最多 120+ 次 | 静态确认 |
| 13 | `translate.ts:377-380`、`:386-391` + `engine.ts:783-786` | 段落级失败写成 `""` 混入成功结果 → 渲染空译文、无失败标记、`stats.error` 不增、错误被静默吞掉 | 静态确认 |
| 14 | `providers/http.ts:168-183` | 流式用**总超时**（60-120s）而非空闲超时：长流被中途砍断，且已 emitted 不回退不重试 | 静态确认 |
| 15 | `translate.ts:889-916` | `splitBatch` 静默剥掉译文里**本就合法**的行首编号：实测 `"1. Introduction"` → `"Introduction"`（哨兵分支无条件 `stripIndex`，行匹配分支 `numbered.some` 条件过宽），结果还会写入缓存 | ✅ 实测复现 |
| 16 | `edgeTts.ts:155-188`、`:273-307` + `index.ts:220-240` | Edge TTS 三个外部请求均无超时、无取消；`tts-synthesize` 无 `beginKeepAlive` → 端点挂起时数分钟无响应且不可取消 | 静态确认 |

### 共享模块与设置（shared/options）

| # | 位置 | 问题 | 验证 |
|---|------|------|------|
| 17 | `langDetect.ts:87` | `ja`/`ko` 分支无密度阈值，一个假名即判日语：实测「这是中文文本，包含一个日语词ソフト。」→ `ja`、「A plain English sentence with a single kana の inside.」→ `ja` | ✅ 实测复现 |
| 18 | `siteRules.ts:221`、`:263-269` | 模式整体 `toLowerCase` 而 `url.pathname` 保持原样 → 大小写敏感/非 ASCII 路径规则永不命中：实测 `github.com/My-Repo`、`example.com/Docs`、`example.com/文档` 全部 false；设置页却宣传「路径前缀 `example.com/docs`」 | ✅ 实测复现 |
| 19 | `siteRules.ts:242-257` | host 通配符拼成正则且不限制 `*` 个数 → 灾难性回溯：实测 `*a` 重复 6 组 121ms、8 组 3170ms（指数增长，14 组即数分钟），在页面主线程执行 | ✅ 实测复现 |
| 20 | `storage.ts:145-146`、`:192` + `extractor.ts:537` | 导入的数值字段无范围校验：`blockMaxChars: 0` 落盘后传入 `splitBySentences` 会先阻塞约 2 秒再抛 `RangeError`（实测 `maxChars=0` → 2011ms 后抛错），每次提取都重复；`timeoutMs: -1000` 经 `scaleTimeoutMs` 钳到 1s，全部请求超时 | ✅ 实测复现 |
| 21 | `storage.ts:279-294` + `popup/main.ts:150-162` | `mergeSettings` 浅合并把 `DEFAULT_SETTINGS` 的**数组引用**放进返回值，popup 就地 `push`/`splice` 污染全局默认值：实测 push 后 `DEFAULT_SETTINGS.sites.whitelist` 变为 `["evil.example.com"]`，后续 `getSettings()` 持续返回被污染值 | ✅ 实测复现 |

---

## P2（可按需修，详见附录）

- `options/main.ts:213`：`timeoutMs: (parseInt(...) || 60) * 1000`，输入 `-1` → `-1000ms`（同文件 `temperature`/`summaryMinChars` 已用 `Number.isNaN` 处理，此处遗漏）
- `options/main.ts:105-110` + `:234-254`：导入无 backupApi 的设置后表单残留旧值，点保存即复活已废弃的备用通道
- `options/main.ts:267-270`：`contextMaxChars` 用 `|| 3000`，用户填 `0`（意图不带上下文）实际存 3000
- `options/main.ts:422-477`、`popup/main.ts:75-120`：测试/清缓存/导出/保存的 async 监听器无 try/catch，失败时界面无任何提示
- `siteRules.ts:248-251`：中段通配过度匹配，实测 `www.amazon.*` 命中 `www.amazon.com.evil.io`、`google.*` 命中 `google.com.evil.io`
- `siteRules.ts:401`、`:418`：只含 `excludeMatches` 的规则、`matches` 为空的规则被静默丢弃，保存仍提示成功
- `siteRules.ts:220-236`：带查询串、带端口、尾斜杠模式的行为与用户直觉不符且无提示
- `shared/messages.ts:108-112`、`:81-84`：`CheckCacheMessage`/`ClearCacheMessage` 无响应类型；`it-tts-play`/`it-tts-stop` 协议完全无类型声明
- `background/index.ts:100` + `content/translate.ts:63` + `translate.ts:466`：魔法字符串 `"cancelled"` 跨模块传递取消语义（4 处），未建模
- `manifest.json:36`：`scripting`、`activeTab` 全仓库零使用（grep 确认）→ 过度授权
- `vite.config.ts:9` + `scripts/zip.mjs`：`sourcemap: true` 未排除 → 发布 zip 含 9 个 `.map`，其中 4 个列入 `web_accessible_resources`，任意 http(s) 页面可按扩展 ID 拉取（实测确认）
- `storage.ts:50`、`:57`、`:116`：`security.encryptApiKey` 无任何消费方（设置项是死的）；`translate.autoDetectSource` 同样无消费点
- `content/tts.ts:62`：`crypto.randomUUID()` 无兜底，而 `extractor.ts:5` 注释明确指出 http 页面不可用、`edgeTts.ts:128` 已做 typeof 兜底 → 三处口径不一致，http 站点朗读静默失效
- `content/perSite.ts:56-61`：`setPageDisabled` 读-改-写无并发保护，多 frame 同时写会互相覆盖
- `content/toolbar.ts:313-323`：位置记忆直写 `left/top` 绕过 `placeFixedInViewport` 的 zoom 校正（`html zoom` 站点刷新后位置偏移）
- `content/bubble.ts:246-256`：`position()` 在内容填充前量 `offsetWidth/Height`（骨架尺寸），译文到达后变宽变高不会重新定位
- `content/content.css:182`、`:209` 等：`.it-translated-hidden` / `.it-mode-*` / `.it-error` 等裸类名 + `!important` 全局作用域，宿主页面撞名即被改样式
- `content/style.ts:28-30`：`getElementById("it-custom-style")` 不校验归属，宿主同名节点会被改写/删除
- `background/translate.ts:220-238`：加入共享去重请求时不接入本会话 signal；`translate.ts:208-210` 去重键不含 context、无碰撞校验
- `providers/http.ts:76-84`：`withErrorSource` 重建 ApiError 时丢 `retryAfterMs`；`http.ts:397-404` `Retry-After: 0` 立即重试
- `background/cache.ts:97-101`、`:236-239`：内存层为插入序 FIFO 而非 LRU；`get()` 回填不做容量检查；`diskKeys` 随写入重复增长
- `providers/googlefree.ts:30-55`、`:113-117`：槽位超发与提前释放；`providers/microsoft.ts:168`、`:171` 段数抖动与 diagnostic 未赋值
- `translate.ts:925-927`：`mapNoTranslationNeeded` 对返回值 `.trim()`，译文首尾空白丢失（实测 `"  indented text  "` → `"indented text"`）
- `edgeTts.ts:252-271`：入参错误（朗读内容为空）也计入熔断失败计数
- `translate.ts:672-683`：`storage.onChanged.addListener` 失败被静默吞掉并置 `storageListenerRegistered = true` → 该 SW 生命周期内设置永不刷新

---

## 误报澄清（已核实为非缺陷）

- **`content/input.ts` 敏感页校验「缺失」**：实际存在（`input.ts:80-89` 的 `focusin` 与 `:100` 的 `mousedown` 均判 `isSensitive?.()`），agent 报告有误。
- **`bubble.ts` 的 `pruneRecent` `break` 假设**：`DEDUP_WINDOW_MS` 固定 3 秒时插入序恰等于时间序，`break` 正确。

## 已核对无问题的高价值项（避免重复审查）

- 跨标签页请求去重的条目清理竞态（`translate.ts:237`、`:603` 的「仅当条目仍指向本次执行时删除」判断正确）
- 缓存哈希碰撞防护（`cache.ts:28-39`、`:203-226` 比对 `src`）
- API Key 脱敏主干（`http.ts:406-429` + `makeDiagnostic` 去 query；Gemini `key=` 已覆盖）
- `keepAlive` 引用计数与定时器清理；`TokenBucket` 无定时器无泄漏
- `masterSwitch.ts` 装配窗口快照对读（`:59` + `:47` 幂等判定）
- `observer.ts` 的「全自身写入即忽略」防自激方向正确（问题仅在匹配范围过宽，见 P1-9）
- `public/offscreen.js` 的 `settle` 幂等、`revokeObjectURL`、blob 清理
- `options/main.ts:145` 唯一 `innerHTML` 仅用于清空；其余用户可控内容全走 `textContent`（无 XSS）
- i18n 中英两表 64 键完全对齐，无缺失键

---

## 附：审查过程与证据

- 四个子系统并行通读 + 主审独立验证；完整分系统报告见审查时的临时产出：
  - background 子系统：`review-background.md`（含逐条触发场景与「已核对无问题」清单）
  - content 引擎/渲染子系统：`review-content-engine.md`
- 主审实测复现脚本（临时，未入库）：storage 导入行为、严格 atob 明文 Key、语言检测、站点规则匹配与 ReDoS、`splitBatch` 编号剥离、`splitBySentences` 边界、`DEFAULT_SETTINGS` 引用污染、`restore()` 孤儿块、`translateAll` 重入。
- 审查为只读，未改动源码；仓库工作区仅含上一轮 `keepAlive` 与 `.gitignore` 修复。
